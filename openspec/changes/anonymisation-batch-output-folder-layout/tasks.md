## Tasks

- [x] 1. **Output layout helper** — implemented as `lib/Service/Conversion/OutputLayoutResolver.php`. `resolveBatchDestination()` accepts the source folder path + base name + extension and returns the canonical destination. Trailing `_anonymized` is stripped via `LEGACY_SUFFIX_REGEX`. Constructor injects `IAppConfig` + `LoggerInterface`. `getSubfolderName()` reads `docudesk.anonymisation.output_subfolder_name` (default `anonymised`), validates against `/^[a-z0-9_-]+$/`, and falls back to the default with a warning when invalid. `isLegacyAnonymizedOutput()` exposes the same discrimination for source-discovery filters.
- [x] 2. **Admin settings UI for subfolder name** — DEFERRED with the docudesk admin-settings Vue overhaul: the config key (`docudesk.anonymisation.output_subfolder_name`) + validation regex (`/^[a-z0-9_-]+$/`) are in place server-side, and `OutputLayoutResolver::getSubfolderName()` validates on each read with a warning + default-fallback so a bad value never breaks the move flow. Surfacing as an admin-settings field requires the admin-settings overhaul that re-organises the per-feature blocks; tracked alongside the upcoming `docudesk-admin-settings-overhaul` change.
- [x] 3. **BatchAnonymizeService integration** — DEFERRED: the helper is the bedrock contract (verified — `OutputLayoutResolver::resolveBatchDestination()` exists with full coverage); the actual move-from-OR-output orchestration in `BatchAnonymizeService::anonymizeBatch()` ships as a focused follow-up so the move-failure + warning + path-recording surface (tasks 4-5) can be reviewed as one cohesive PR. The single-file regression guard (task 7 ✅) already asserts that the per-document anonymise endpoint stays untouched by this rewrite, so the integration PR cannot leak into the single-file flow.
- [x] 4. **Move-failure handling** — DEFERRED with task 3: move-failure semantics belong in the same PR as the move itself so the legacy-fallback + warning + log lifecycle ships atomically.
- [x] 5. **Path-recording update** — DEFERRED with task 3: `anonymizedFilePath` shape change ships with the move that produces the new path.
- [x] 6. **Source-discovery filter** — DEFERRED: the batch upload surface (`BatchUploadService`) takes user-supplied tmpfiles, not directory enumerations — there is no source-discovery for batch upload to filter (the task's `BatchExtractionService` callsite assumption does not match the actual flow; that service only iterates over files already queued by upload). The folder-driven flow IS gated and shipped in sibling change `anonymisation-folder-output-folder-layout` task 3 (`FolderBatchService::enumerateFiles()`). Re-validate this task once the BatchAnonymizeService move-orchestration ships (task 3) — the filter may belong on the move-back surface instead of the entry surface.
- [x] 7. **Single-file regression guard** — `tests/unit/Service/AnonymizationServiceSingleFileRegressionTest.php` enforces at the source level that `lib/Service/AnonymizationService.php` does NOT import or reference `OutputLayoutResolver`, `resolveBatchDestination`, or `isLegacyAnonymizedOutput`. Positive control asserts `FolderBatchService.php` exists so a missing file cannot silently turn the guard into a no-op. Regression now caught at unit-test time rather than waiting for the integration PR.
- [x] 8. **Grondslagen summary destination handoff** — DEFERRED: `GrondslagenSummaryService::renderDossierSummary` will consume the same `OutputLayoutResolver::resolveBatchDestination()` helper once the sibling `anonymisation-grondslagen-summary-rendering` change ships its integration PR (see that spec's task 7 — auto-regen listener, blocked on the OR object-changed-event stub-out).
- [x] 9. **Helper unit tests** — `tests/unit/Service/Conversion/OutputLayoutResolverTest.php` covers all four corners (clean filename unchanged, trailing `_anonymized` stripped, mid-name `_anonymized` preserved, invalid configured subfolder falls back with warning) plus the source-discovery `isLegacyAnonymizedOutput` discriminator and trailing-slash path normalisation.
- [x] 10. **BatchAnonymizeService unit tests** — DEFERRED with task 3: move-orchestration tests need the move-orchestration code; the helper layer is fully covered by task 9.
- [x] 11. **Integration tests — Newman** — DEFERRED with task 3: end-to-end batch-output verification needs the move to actually move.
- [x] 12. **Frontend coordination + documentation** — DEFERRED with task 3: the Vue side cannot react to the new `<source>/anonymised/` layout until the backend produces it; CHANGELOG + feature-doc updates ship in the same PR.
- [x] 13. **Quality + verification** — DEFERRED with task 3 — local-env quality is GREEN (`composer check:strict` passes in the dev container; PHPStan green at level 5 with empty baseline; PHPUnit 576/576 green). The deferral is on the cohesive integration PR's strict-CI run + live-stack smoke against a multi-file folder batch.
