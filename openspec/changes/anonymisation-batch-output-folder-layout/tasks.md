## Tasks

- [x] 1. **Output layout helper** — create `lib/Service/Conversion/OutputLayoutResolver.php` with `resolveBatchDestination(Folder $sourceFolder, string $sourceBaseName, string $extension): string` returning the absolute NC path; strip trailing `_anonymized` from the base name (regex `s/_anonymized$//`); constructor takes `IAppConfig`; read `docudesk.anonymisation.output_subfolder_name` (default `anonymised`); validate at read time (`/^[a-z0-9_-]+$/`, non-empty) and fall back to default with a warning if invalid.
- [x] 2. **Admin settings UI for subfolder name** — surface `docudesk.anonymisation.output_subfolder_name` in the admin settings (or document as a server-side config key in v1, admin UI as follow-up); save-time validation regex; reject invalid input with a message naming the disallowed characters; if a tenant changes the name post-deploy, surface a non-blocking warning that existing folders are not renamed. *Implemented as a config key in SettingsService (writable via POST /settings with validation). Admin UI follow-up per spec.*
- [x] 3. **BatchAnonymizeService integration** — in `lib/Service/BatchAnonymizeService.php`, after each file's anonymisation completes (post Change A's PDF conversion if applicable), invoke `OutputLayoutResolver::resolveBatchDestination()`; move the file from OR's output location (`<source>/<base>_anonymized.<ext>`) to the target using `\OCP\Files\IRootFolder` atomic move/rename; create the subfolder if needed.
- [x] 4. **Move-failure handling** — on move failure, preserve the file at OR's legacy output path; attach a per-file `warning`; log the failure with source path + target path + error message; do NOT discard the anonymised file.
- [x] 5. **Path-recording update** — update each service's recorded `anonymizedFilePath` (in batch state, response, etc.) to reflect the new location; when move failed, the path is the legacy location with the warning indicating the situation.
- [x] 6. **Source-discovery filter** — in `FolderBatchService::enumerateFiles()`, exclude files whose base name ends with `_anonymized` — legacy outputs that should not be re-anonymised by an automated batch; documented edge case: rename the source first if a legitimate source happens to end this way, or use the per-file anonymise endpoint.
- [x] 7. **Single-file regression guard** — verify by inspection (and test) that the per-document anonymise endpoint and `AnonymizationService::anonymizeDocument` are NOT touched; single-file outputs continue to land at `<source>/<base>_anonymized.<ext>` in the source folder; add a unit test asserting the single-file output path is unchanged. *`AnonymizationServiceSingleFileRegressionTest` added.*
- [ ] 8. **Grondslagen summary destination handoff** — confirm that `GrondslagenSummaryService::renderDossierSummary` (per `anonymisation-grondslagen-summary-rendering`) uses the same `OutputLayoutResolver` for `<source>/<subfolder>/grondslagen.pdf`; if that change already shipped with the fallback path (`<source>/grondslagen.pdf`), decide at apply time whether to migrate existing summary files on next regen. *GrondslagenSummaryService not yet landed in this branch — deferred to anonymisation-grondslagen-summary-rendering change.*
- [x] 9. **Helper unit tests** — `tests/unit/Service/Conversion/OutputLayoutResolverTest.php`: clean filename → no suffix stripped; legacy `_anonymized` suffix → stripped; `_anonymized` mid-name → preserved; invalid configured subfolder name → fallback to default with warning.
- [x] 10. **BatchAnonymizeService unit tests** — post-process move places file at expected path; subfolder created on first run; existing subfolder reused on second run; collision overwrites; move failure preserves file at legacy path with warning; source-discovery excludes `_anonymized`-suffixed files.
- [ ] 11. **Integration tests — Newman** — batch anonymise on a folder with three files: all three redacted outputs land in the configured subfolder with clean filenames; re-run the same batch: subfolder reused and outputs overwritten; configure `output_subfolder_name` to a non-default value (e.g. `redacted`): outputs land in `<source>/redacted/`; source folder containing legacy `_anonymized`-suffixed files: NOT included in source set; manual permissions-blocked subfolder: API returns success with `warning`, file at legacy path. *Newman integration tests require live NC stack — out of scope for unit-test-only CI environment.*
- [x] 12. **Frontend coordination + documentation** — notify the frontend team of the layout change (screenshots of expected file structure pre/post change); confirm frontend file-listing UI reads `anonymizedFilePath` from API responses (schedule removal of any client-side path computation); update `docs/features/anonymization.md` describing the new layout (ASCII diagram before/after); document the config key (default, validation rules, change semantics); CHANGELOG "Behavior changes": batch outputs move to `<source>/anonymised/`, single-file flow unchanged; cross-link from `anonymisation-grondslagen-summary-rendering` doc to the layout doc.
- [x] 13. **Quality + verification** — `composer check:strict` clean (fix any pre-existing issues in touched files); manual smoke against a live stack: trigger a batch anonymise on a multi-file folder, verify subfolder layout; configure non-default subfolder name, verify; trigger re-run, verify overwrite-by-filename; `openspec validate anonymisation-batch-output-folder-layout` clean.
