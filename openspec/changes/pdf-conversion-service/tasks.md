## Tasks

- [ ] 1. **Composer dependency** — add `phpoffice/phpword` (`^1.2`, matching OR's pin) to `composer.json` under `require`; run `composer update phpoffice/phpword` and commit `composer.lock`; confirm `mpdf/mpdf` is already available (from `pdf-generation`); confirm `composer install` in CI has no conflict resolutions.
- [ ] 2. **Service skeleton + cascade** — create `lib/Service/PdfConversionService.php`; constructor injects ordered `ConversionBackendInterface` list + logger + `IAppConfig`; implement `convertToPdf(File $source, array $opts = []): File` walking the cascade (`isAvailable()` then `canHandle($mime, $ext)` then `convert()`); first success returns; all failure throws `ConversionFailedException`.
- [ ] 3. **Interface + exception** — create `lib/Service/Conversion/ConversionBackendInterface.php` (`canHandle(string $mime): bool`, `convert(File $source): File`, `isAvailable(): bool`, `name(): string`); create `lib/Exception/ConversionFailedException.php` with `getAttempts(): array` returning `[{name, available, supports, reason}, ...]`.
- [ ] 4. **MpdfBackend** — `lib/Service/Conversion/MpdfBackend.php`: handles HTML + TXT (wraps TXT in `<pre>` HTML envelope); configures mPDF for PDF/A-3b output (`'PDFA' => true`); `name()`: `mpdf`.
- [ ] 5. **PhpWordBackend** — `lib/Service/Conversion/PhpWordBackend.php`: uses `\PhpOffice\PhpWord\IOFactory::load()` to read DOCX/ODT/RTF/HTML; configures `Settings::setPdfRendererName(Settings::PDF_RENDERER_MPDF)` + `Settings::setPdfRendererPath()`; emits PDF/A-3b via `IOFactory::createWriter($phpWord, 'PDF')`; `canHandle()` returns true for those four MIMEs; `name()`: `phpword`.
- [ ] 6. **LibreOfficeHeadlessBackend** — `lib/Service/Conversion/LibreOfficeHeadlessBackend.php`: invokes `soffice --headless --convert-to "pdf:writer_pdf_Export:UseTaggedPDF=true,SelectPdfVersion=2" --outdir <tmp> <input>`; acquires `ILockingProvider` lock keyed `soffice:headless:convert` before invocation and releases after; `isAvailable()` checks the configured binary path resolves + executable; `name()`: `libreoffice_headless`.
- [ ] 7. **OfficeAppBackend** — `lib/Service/Conversion/OfficeAppBackend.php`: wraps Collabora and OnlyOffice convert APIs; detects which is installed/configured via NC's app-manager + each app's settings; uses the appropriate API to request PDF/A-3b output; `name()`: `office_app`.
- [ ] 8. **EmlBackend (placeholder)** — `lib/Service/Conversion/EmlBackend.php`: `isAvailable()` returns true only when OR's `TextExtractionService` supports `message/rfc822` (probe via reflection or feature flag); `convert()` extracts body, wraps in HTML, delegates to mPDF backend; `name()`: `eml`; until OR's EML support lands, `isAvailable()` returns false.
- [ ] 9. **DI registration** — register all backends in `Application::register()` (or equivalent DI registration point) in the documented cascade order: `OfficeAppBackend`, `LibreOfficeHeadlessBackend`, `PhpWordBackend`, `MpdfBackend`, `EmlBackend`.
- [ ] 10. **Tenant configuration** — define `default_output_format`, per-backend `*_enabled`, `libreoffice_binary_path`, `timeout_seconds` in admin settings UI (or as undocumented IAppConfig keys in v1 with admin UI follow-up); wire each backend's `isAvailable()` to read its `*_enabled` flag (default true); wire `LibreOfficeHeadlessBackend` to read `libreoffice_binary_path` (default `soffice`); wire all backends' `convert()` to enforce `timeout_seconds` (default 60) — `proc_open` + select loop for shell-out; deadline check for in-process.
- [ ] 11. **Backend unit tests** — `MpdfBackendTest`, `PhpWordBackendTest`, `LibreOfficeHeadlessBackendTest`, `OfficeAppBackendTest`, `EmlBackendTest`: each covers supported MIMEs, rejected MIMEs, `isAvailable()` paths, PDF/A-3b output verification; for shell-out backends also assert lock acquisition + timeout enforcement + non-zero-exit handling.
- [ ] 12. **Service unit tests** — `tests/unit/Service/PdfConversionServiceTest.php`: cascade walks in declared order; first-success short-circuits; `ConversionFailedException` aggregates per-backend `attempts`; tenant config disables backends correctly.
- [ ] 13. **Integration tests** — Newman / functional: with LibreOffice or PhpWord available, convert a DOCX → PDF/A-3b (verify via `pdfinfo`); with all backends disabled, the service throws `ConversionFailedException` with all `attempts` populated; manual: with Collabora installed, the Office app backend wins (document test rather than automate Collabora setup in CI).
- [ ] 14. **Documentation + quality** — document the new dep + cascade in `docs/features/pdf-conversion.md` (create if missing); CHANGELOG "Added": new `PdfConversionService` with cascade backends + tenant configuration for backend control; document the soft cross-app dep on OR's future `text-extraction-eml`; `composer check:strict` clean (fix pre-existing issues in touched files); `openspec validate pdf-conversion-service` clean.
