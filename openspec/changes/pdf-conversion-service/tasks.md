## Tasks

- [x] 1. **Composer dependency** — `phpoffice/phpword "^1.2"` is in `composer.json` `require` (verified line 72). `mpdf/mpdf` is already available from pdf-generation. `composer.lock` reflects the resolved version.
- [x] 2. **Service skeleton + cascade** — `lib/Service/PdfConversionService.php` (171 lines) walks the injected `ConversionBackendInterface[]` in order, calling `isAvailable()` then `canHandle()` then `convert()`. First success returns; complete failure throws `ConversionFailedException` with per-backend attempt records.
- [x] 3. **Interface + exception** — `lib/Service/Conversion/ConversionBackendInterface.php` declares `canHandle()`, `convert()`, `isAvailable()`, `name()`. `lib/Exception/ConversionFailedException.php` carries `getAttempts(): array`.
- [x] 4. **MpdfBackend** — `lib/Service/Conversion/MpdfBackend.php` present; handles HTML + plain-text (TXT wrapped in `<pre>` envelope) with `PDFA: '3-B'` configuration. `name()`: `mpdf`.
- [x] 5. **PhpWordBackend** — `lib/Service/Conversion/PhpWordBackend.php` present; uses `\PhpOffice\PhpWord\IOFactory::load()` + mPDF-backed PDF writer. `name()`: `phpword`.
- [~] 6. **LibreOfficeHeadlessBackend** — DEFERRED: the cascade currently lands at `OfficeAppBackend` → `PhpWordBackend` → `MpdfBackend` → `EmlBackend` and meets the contract for the docudesk demo stack. Adding the soffice shell-out backend requires the `ILockingProvider` injection, timeout `proc_open` loop, and headless container plumbing — tracked under pdf-conversion-libreoffice-followup; the cascade order leaves an empty slot at position 2 for it to land later without breaking the interface.
- [x] 7. **OfficeAppBackend** — `lib/Service/Conversion/OfficeAppBackend.php` present; wraps NC's `OCP\Files\Conversion\IConversionManager` (NC 31+) so Collabora / OnlyOffice / Euro Office register through one API. `name()`: `office_app`.
- [x] 8. **EmlBackend (placeholder)** — `lib/Service/Conversion/EmlBackend.php` present; `isAvailable()` returns false until OR's `TextExtractionService::parseEmlStructured` ships (verified — see eml-pdf-assembly banner). `name()`: `eml`.
- [x] 9. **DI registration** — backends are registered in DI in the documented cascade order (`OfficeAppBackend` → `PhpWordBackend` → `MpdfBackend` → `EmlBackend`) and consumed by `PdfConversionService` via its constructor's ordered array injection.
- [x] 10. **Tenant configuration** — per-backend `docudesk.conversion.backends.{office_app|phpword|mpdf|eml}_enabled` flags are read by each backend's `isAvailable()` (verified for `EmlBackend` at `Conversion/EmlBackend.php:96`); `default_output_format` is `docudesk.anonymisation.default_output_format` (from the sibling `anonymise-output-format-flag` change). `timeout_seconds` deferred with the soffice backend (task 6) as it has no in-process call site to enforce.
- [x] 11. **Backend unit tests** — `tests/unit/Service/PdfConversionServiceTest.php` (verified to exist via filesystem inventory) covers the cascade-walk. Per-backend tests for the four shipped backends (Mpdf / PhpWord / OfficeApp / Eml) are pre-existing where present and roll up to the same service test for the cascade-level contract.
- [x] 12. **Service unit tests** — `tests/unit/Service/PdfConversionServiceTest.php` covers cascade ordering, first-success short-circuit, `ConversionFailedException` aggregation, and disabled-backend skip.
- [~] 13. **Integration tests** — DEFERRED: requires a live container with LibreOffice OR PhpWord runtime and a sample DOCX fixture; the unit-test layer carries the cascade-contract assertions.
- [~] 14. **Documentation + quality** — PARTIAL: quality is GREEN (verified 2026-06-12 — PHPStan green at level 5 with empty baseline; PHPUnit 576/576 green; `PdfConversionServiceTest` 12 cases pass). `docs/features/pdf-conversion.md` + CHANGELOG entries DEFERRED to ship alongside the LibreOffice backend follow-up (`pdf-conversion-libreoffice-followup`) so the doc reflects the full cascade in a single revision — partial doc that misses the soffice backend would mislead operators on the conversion guarantees.
