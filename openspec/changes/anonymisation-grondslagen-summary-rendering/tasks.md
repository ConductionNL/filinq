## Tasks

- [x] 1. **GrondslagenSummaryService skeleton** — `lib/Service/GrondslagenSummaryService.php` (250+ lines) ships with the documented public surface: `appendSummaryToPdf(File, int): File` (line 137), `renderSummaryBesideFile(File, int): File` (line 185), `renderDossierSummary(string $dossierUuid): File` (line 246). Constructor wires `PdfService`, the OR object service + entity-relation mapper, the dossier mapper, file service, and logger.
- [x] 2. **Twig templates** — `lib/Resources/templates/grondslagen/summary_per_doc.twig` and `summary_per_dossier.twig` are present and rendered through `PdfService`'s Twig sandbox; mPDF PDF/A-3b is configured per the print-preview baseline.
- [x] 3. **Per-document append flow** — `appendSummaryToPdf(File $anonymisedFile, int $sourceFileId): File` (line 137) implements the `outputFormat: "pdf"` case via Twig + PdfService + mPDF/FPDI source-page import + appended summary page. Used by the sibling `anonymisation-append-basis-summary-flag` (task 2).
- [x] 4. **Preserve-mode separate-PDF path** — `renderSummaryBesideFile(File $anonymisedFile, int $sourceFileId): File` (line 185) writes `<original-base>_anonymized_grondslagen.pdf` next to the preserved native output. Used by the sibling spec's task 5.
- [x] 5. **Per-dossier summary endpoint** — `lib/Controller/DossierController.php` exposes the endpoint and `appinfo/routes.php` line 61 registers `POST /api/anonymization/dossier/{dossierId}/grondslagen-pdf` → `dossier#generateGrondslagenSummary`. Destination resolution follows the dossier's `@self.folder`.
- [x] 6. **Per-dossier aggregation + write** — `renderDossierSummary(string $dossierUuid): File` (line 246) walks the dossier folder, aggregates per-document + per-grondslag, renders the dossier-summary template, writes the file, and updates the dossier's `configuration.grondslagen.fileId` + `lastGeneratedAt`.
- [~] 7. **Auto-regen listener** — DEFERRED: the on-demand endpoint is shipped + tested; the OR object-changed-event subscription that fires `renderDossierSummary` synchronously on a `dossier.checkedOn` write needs the OR event-listener stub-out before it can be unit-tested honestly. Tracked as `grondslagen-auto-regen-listener` follow-up.
- [x] 8. **Dossier `configuration.grondslagen` fields** — `configuration` is free-form JSON per `add-dossier-schema`, so no schema migration. The on-demand path writes `configuration.grondslagen.fileId` + `lastGeneratedAt`; `autoRegenOnReview` is read with default `true` by the listener follow-up (task 7).
- [x] 9. **Service unit tests** — `tests/unit/Service/GrondslagenSummaryServiceTest.php` is present (verified via filesystem inventory) and covers the base-resolution + aggregation + rendering-failure scenarios.
- [~] 10. **Controller + listener unit tests** — Controller tests ship alongside `DossierController` (verified); listener tests deferred with task 7.
- [~] 11. **Integration tests — Newman** — DEFERRED: requires a seeded dossier fixture in the live container; service- and controller-level coverage carries the contract assertions.
- [~] 12. **Documentation** — DEFERRED: feature doc `docs/features/grondslagen-summary.md` lands alongside the listener follow-up so the on-demand + auto-regen surfaces are documented together.
- [~] 13. **Quality + verification** — DEFERRED: `composer check:strict` and manual PDF/A-3b conformance run against the dev container; the service + endpoint are in place.
