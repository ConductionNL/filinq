## Tasks

- [x] 1. **GrondslagenSummaryService skeleton** — create `lib/Service/GrondslagenSummaryService.php`; constructor injects `PdfService`, OR `EntityRelationMapper`, OR object service (for resolving `base` records), the dossier mapper, the file service, and the logger; define two public methods `appendSummaryToPdf(File $anonymisedFile, int $sourceFileId): File` and `renderDossierSummary(int $dossierId): File`; helpers `resolveBaseLabels(array $baseUuids): array` (placeholder `⟨grondslag verwijderd: <short-uuid>⟩` for unresolved) and `loadAnonymisedEntitiesForFile(int $fileId): array` (filters `anonymized = true`).
- [x] 2. **Twig templates** — create `lib/Resources/templates/grondslagen/summary_per_doc.twig` (header: filename, anonymised-at, operator, tool; per-entity table: entityText, entityType, anonymizedValue, bases; footer: entity count + distinct bases count; NL labels) and `summary_per_dossier.twig` (header: dossier name/description/checkedOn/generatedAt; per-document + per-grondslag tables; aggregate-totals footer; NL labels); verify both render through `PdfService`'s sandbox (expand safe-tag list narrowly if anything is blocked); configure mPDF for PDF/A-3b for both (mirror `print-preview`).
- [x] 3. **Per-document append flow** — implement `appendSummaryToPdf` for the `outputFormat: "pdf"` case: render summary via Twig + PdfService → temp PDF; open the anonymised PDF via mPDF + FPDI; iterate source pages with `setSourceFile` + `importPage` + `useTemplate`; append a new page with summary content; output to temp file; replace the anonymised file in NC atomically; on render/append throw, propagate the exception (the controller in the sibling change handles the warning surface).
- [x] 4. **Preserve-mode separate-PDF path** — implement the `outputFormat: "preserve"` path: render summary → save as `<original-base>_anonymized_grondslagen.pdf` in the same folder as the anonymised file; return both file references; verify mPDF's `setSourceFile`/`importPage`/`useTemplate` work against PDF/A-3b inputs (spike during apply if needed).
- [x] 5. **Per-dossier summary endpoint** — add `lib/Controller/DossierController.php` (or extend) with route `POST /api/anonymization/dossier/{dossierId}/grondslagen-pdf` (authenticated, no body); resolve the dossier via OR, read `@self.folder`; destination: `<dossier-folder>/anonymised/grondslagen.pdf` (per `anonymisation-output-folder-layout`), fallback `<dossier-folder>/grondslagen.pdf`.
- [x] 6. **Per-dossier aggregation + write** — walk all files under the dossier folder; for each, call `loadAnonymisedEntitiesForFile`; aggregate per-document and per-grondslag; render `summary_per_dossier.twig` via PdfService; save to the destination (overwrite if exists); update the dossier's `configuration.grondslagen.fileId` + `lastGeneratedAt` via OR; return HTTP 200 with file metadata; empty-dossier case: still generate a near-empty PDF (header + empty tables + zero totals) and return.
- [x] 7. **Auto-regen listener** — subscribe to OpenRegister object-changed events for the `dossier` register; on a write that updates `dossier.checkedOn`, check `dossier.configuration.grondslagen.autoRegenOnReview` (default `true`); if true, invoke `renderDossierSummary($dossierId)` synchronously within the same transaction; if regen throws, log + continue (the review MUST succeed); confirm by inspection that a `checkedOn` update fires exactly one regen.
- [x] 8. **Dossier `configuration.grondslagen` fields** — document the keys (`fileId` int|null, `lastGeneratedAt` ISO-8601 string|null, `autoRegenOnReview` bool default true); no schema migration required (`configuration` is free-form JSON per `add-dossier-schema`); both on-demand and auto-regen paths write these fields after a successful regen; extend `docs/features/dossier-register.md` once it lands.
- [x] 9. **Service unit tests** — `tests/unit/Service/GrondslagenSummaryServiceTest.php`: base resolution (resolved, unresolved, null bases); per-doc append with mock anonymised PDF; per-dossier render with multi-file aggregation; empty-dossier case; rendering failure surfaces as exception.
- [x] 10. **Controller + listener unit tests** — `tests/unit/Controller/DossierControllerTest.php`: endpoint returns file metadata, file written at expected path, configuration fields updated, empty dossier handled; listener test for `checkedOn` update: auto-regen fires when `autoRegenOnReview` is true, skipped when false, failure logged but dossier update succeeds.
- [ ] 11. **Integration tests — Newman** — `POST .../dossier/{dossierId}/grondslagen-pdf` verifies response shape, file written at destination, dossier `configuration.grondslagen` updated; updating dossier `checkedOn` via PUT triggers auto-regen (re-fetch dossier; `lastGeneratedAt` updated, file overwritten); setting `autoRegenOnReview: false` + updating `checkedOn` does NOT regen.
- [x] 12. **Documentation** — add `docs/features/grondslagen-summary.md` describing both surfaces, destination-path conventions (with the folder-layout dependency note), auto-regen behaviour, and the `configuration.grondslagen.*` fields; CHANGELOG "Added": new per-dossier summary endpoint + auto-regen on dossier `checkedOn`; CHANGELOG note cross-change deps (`entity-relation-grondslagen`, `add-dossier-schema`, soft deps on `anonymise-output-as-pdf-by-default` and `anonymisation-output-folder-layout`); document NL-only template limitation (EN follows `register-i18n`).
- [ ] 13. **Quality + verification** — `composer check:strict` clean (fix any pre-existing issues in touched files); manual smoke against a live stack: configure a dossier, anonymise files, trigger the per-dossier endpoint, verify the dossier-folder summary PDF, update `checkedOn` and verify auto-regen; `openspec validate anonymisation-grondslagen-summary-rendering` clean; manual PDF/A-3b conformance check on both summary outputs (`pdfinfo` or `verapdf`).
