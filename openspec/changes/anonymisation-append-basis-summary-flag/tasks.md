## Tasks

- [x] 1. **Payload validation** — update `lib/Controller/AnonymizationController::anonymize` to read optional top-level `appendBasisSummary` (boolean, default `false`); validate type and reject malformed input with HTTP 400; mirror on `lib/Controller/BatchAnonymizationController` for the batch endpoint.
- [x] 2. **Service orchestration — per-document** — in `lib/Service/AnonymizationService::anonymizeDocument`, after the anonymised file has been written (post conversion), if `appendBasisSummary` is `true` call `GrondslagenSummaryService::appendSummaryToPdf()` (PDF output) or the preserve-mode separate-PDF path (preserve output) per `anonymisation-grondslagen-summary-rendering`.
- [x] 3. **Service orchestration — batch** — update `lib/Service/BatchAnonymizeService` to honour the flag per file; per-file warnings are collected; the batch overall succeeds even if individual summaries fail; response carries per-file warning array.
- [x] 4. **Warning response shape** — on summary failure, log the error, attach a structured `warning` field to the response (stable error code suitable for frontend display), return HTTP 200 with the anonymised file metadata; anonymised file MUST be preserved as-is; no partial summary PDF left in NC.
- [x] 5. **Preserve-mode response shape** — `outputFormat: "preserve"` + `appendBasisSummary: true` returns both file references (anonymised file metadata + `summaryFileId` / `summaryFilePath` for the separate `<original-base>_anonymized_grondslagen.pdf`).
- [x] 6. **Missing-grondslag handling** — files in a batch whose `EntityRelation.bases` is empty / null still get a summary page that lists their anonymised entities with the `⟨geen grondslag vastgelegd⟩` placeholder (the rendering service handles this; this change just calls it).
- [x] 7. **Unit tests — controller** — `tests/unit/Controller/AnonymizationControllerTest.php` extension: flag defaults to false (no summary work); flag true + PDF mode invokes the append path; flag true + preserve mode invokes the separate-PDF path; rendering exception surfaces as `warning` field with 200 status; payload validation rejects non-boolean.
- [x] 8. **Unit tests — service orchestration** — `tests/unit/Service/AnonymizationServiceTest.php` extension: append called exactly once when the flag is true; not called when omitted/false; batch service iterates per-file and aggregates warnings.
- [ ] 9. **Integration tests — Newman** — per-document anonymise with `appendBasisSummary: true` + PDF output: resulting file has an extra page with the rendered summary (assert on PDF text or page count); per-document with preserve + flag: two files exist in target folder (anonymised native + separate `_grondslagen.pdf`); rendering failure: HTTP 200 + `warning` field present, anonymised file at expected path.
- [x] 10. **Documentation** — update `docs/features/anonymization.md` (or cross-link from `docs/features/grondslagen-summary.md`): `appendBasisSummary` flag, opt-in mechanics, warning shape on failure, preserve-mode separate-PDF behaviour; CHANGELOG "Added": `appendBasisSummary` flag on per-document and batch anonymise endpoints.
- [x] 11. **Quality + verification** — `composer check:strict` clean (fix any pre-existing issues in touched files); manual smoke against a live stack: anonymise files with the flag, verify the appended summary page and the preserve-mode separate-PDF case; `openspec validate anonymisation-append-basis-summary-flag` clean.
