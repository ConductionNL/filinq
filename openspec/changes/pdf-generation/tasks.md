## Tasks

- [x] 1. **Composer dependencies** — add `mpdf/mpdf: ^8.2` and `twig/twig: ^3.18` to `composer.json`; run `composer install` and confirm both packages resolve without conflict with existing DocuDesk dependencies; commit the updated `composer.lock`.

- [x] 2. **TemplateRenderer service** — create `lib/Service/TemplateRenderer.php`; constructor injects the Nextcloud logger; implement `renderTemplate(string $templateContent, array $data): string`; configure a Twig `ArrayLoader` with the template stored under key `"document"`; create a `SecurityPolicy` with the 23 allowed filters, 5 allowed functions, 10 allowed tags, and zero allowed methods/properties; add a `SandboxExtension(policy, sandboxed: true)` to a new `Environment(loader, ['strict_variables' => false])`; call `$twig->render('document', $data)`; wrap `Twig\Error\Error` in a new `Exception` with message "Template rendering failed: {original message}".

- [x] 3. **PdfService** — create `lib/Service/PdfService.php`; constructor injects `TemplateRenderer` and the Nextcloud logger; implement `renderPdf(string $templateContent, array $data = [], array $options = []): string`; before creating the mPDF instance, ensure `/tmp/mpdf` exists with `0777` permissions (`mkdir` if absent, `chmod` always); resolve options from `$options` with defaults (format: `"A4"`, orientation: `"P"`, margins: `15` mm all sides); instantiate `\Mpdf\Mpdf` with the resolved config; call `$this->templateRenderer->renderTemplate($templateContent, $data)`; write the HTML via `$mpdf->WriteHTML()`; set `$mpdf->SetTitle()` when `title` option is present; capture PDF with `$mpdf->Output('', 'S')`; catch `\Mpdf\MpdfException`, log "mPDF generation failed", re-throw as `Exception` with code 500; add `@spec openspec/changes/pdf-generation/tasks.md#task-3` to file and class docblock.

- [x] 4. **PdfController** — create `lib/Controller/PdfController.php`; extend `OCP\AppFramework\Controller`; constructor injects `IRequest` and `PdfService`; implement `render(): DataDownloadResponse|JSONResponse`; read `template`, `data`, `options`, `filename` from `$this->request->getParam()`; return HTTP 400 `JSONResponse(['message' => 'template is required'])` if `template` is absent or empty; call `$this->pdfService->renderPdf($template, $data ?? [], $options ?? [])`; return `new DataDownloadResponse($pdfBinary, $filename ?? 'document.pdf', 'application/pdf')`; annotate with `#[NoAdminRequired]` (authenticated non-admin users may generate PDFs); no per-object IDOR risk (stateless, no stored objects); add `@spec` docblock tag.

- [x] 5. **Route registration** — add route to `appinfo/routes.php`: `['name' => 'pdf#render', 'url' => '/api/pdf/render', 'verb' => 'POST']`; ensure it appears before any wildcard `{slug}` routes per ADR-003.

- [x] 6. **Deduplication check** — search `openregister/lib/Service/` and existing DocuDesk services for any existing PDF generation, Twig rendering, or mPDF wrapper; document findings; confirm no overlap (result: no prior implementation found — both libraries and service layer are net-new).

- [x] 7. **Unit tests — TemplateRenderer** — `tests/Unit/Service/TemplateRendererTest.php`: test (a) valid template with data renders correctly; (b) static HTML with empty data renders without error; (c) broken Twig syntax throws `Exception` with "Template rendering failed" in the message; (d) forbidden function `system` triggers `SecurityError` caught and re-thrown; (e) object method access blocked; (f) `{% for %}` loop over array data renders correctly.

- [x] 8. **Unit tests — PdfService** — `tests/Unit/Service/PdfServiceTest.php`: mock `TemplateRenderer`; test (a) successful render returns non-empty string; (b) default options produce A4/portrait/15mm config; (c) custom format/orientation options are passed to mPDF; (d) title option calls `SetTitle`; (e) `TemplateRenderer` exception propagates as-is; (f) `MpdfException` is logged and re-thrown with code 500; (g) temp directory creation is attempted before mPDF instantiation.

- [x] 9. **Unit tests — PdfController** — `tests/Unit/Controller/PdfControllerTest.php`: mock `PdfService`; test (a) missing template returns HTTP 400 with correct message; (b) empty template string returns HTTP 400; (c) valid request returns `DataDownloadResponse` with `application/pdf` MIME; (d) filename defaults to `document.pdf` when absent; (e) custom filename is used when provided; (f) options are forwarded to `PdfService`; (g) `PdfService` exception propagates as HTTP 500.

- [x] 10. **Integration tests — Newman** — add collection `tests/integration/pdf-generation.json`; cover: (a) `POST /apps/docudesk/api/pdf/render` with valid template and data → 200 + `application/pdf` response; (b) missing `template` field → 400 with error message; (c) custom page options (`A3` landscape) → 200; (d) unauthenticated request → 401; use env variable placeholders for credentials — no hardcoded defaults.

- [x] 11. **Documentation** — create `docs/features/pdf-generation.md` describing the service API (`renderPdf` signature, options table, error contract), the Twig sandbox whitelist, the HTTP endpoint request/response format, and a usage example for co-installed apps calling `PdfService` via DI; add CHANGELOG entry "Added: shared PDF rendering service (`PdfService`) and `POST /api/pdf/render` endpoint".

- [x] 12. **Quality + verification** — `composer check:strict` clean on all new files; manual smoke test: call the endpoint with `curl`, verify `application/pdf` response, verify broken template returns 400; verify mPDF temp directory is created at `/tmp/mpdf`; confirm `PdfService` is resolvable from the DI container in a running Nextcloud instance.
