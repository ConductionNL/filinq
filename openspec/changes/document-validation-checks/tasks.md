## Tasks

- [ ] 1. **Truthfulness fixes (immediate)** — edit `docs/GOVERNMENT-FEATURES.md` row F-02: *Beschikbaar* → *Gepland (in ontwikkeling)* with a pointer to this change; **delete `docs/api/full-text-search.md`** (stale Solr doc, zero corresponding code; search is OR's domain per ADR-022) and add a one-line pointer in the docs index to OR/NC unified search. Independent of the build, ships first.
- [ ] 2. **`DocumentValidationService` — check catalogue** — implement the five check families: `format-not-allowed`, `extension-mime-mismatch`, `file-unreadable` (extractor parse failure), `pdf-encrypted`, `text-layer-missing` (avg < `docudesk.validation.text_layer_min_chars_per_page`, default 32), `metadata-incomplete` (required fields per profile). Pure computation: no field writes, no I/O beyond reading the file + record.
- [ ] 3. **Validation profiles** — app config `docudesk.validation.profiles` (JSON: per document type → allowed mimes, required metadata fields, per-check severity `off|warning|blocking`); shipped defaults warn-only with a sensible mime allowlist; profile resolution falls back to the `default` profile for unknown document types.
- [ ] 4. **Verdict as OR calculation** — declare `validationStatus` + `validationFindings` as `x-openregister-calculations` on the document/report schemas in `lib/Settings/docudesk_register.json`, computation backend = `DocumentValidationService` (mirror REQ-META-CAL); event-listener fallback dispatch until OR's ADR-031 runtime ships, listener contains no validation logic.
- [ ] 5. **On-demand endpoint** — `POST /api/validation/validate` (`{fileId, documentType?}`), `#[NoAdminRequired]` + per-file access guard, returns findings + verdict without persisting; route registered (gate-14), 404 on inaccessible file.
- [ ] 6. **Pipeline gate** — upload/extract paths (single, batch, folder) run validation; blocking findings yield HTTP 422 with the findings list and abort intake of that file (batch/folder: skip the file, record the finding on the batch report, continue the batch); warning findings ride along on the success response.
- [ ] 7. **Admin settings UI** — validation profiles section under DocuDesk admin settings: per-type profile editor, per-check severity selectors, "blocking checks active" summary banner; NcSelect inputs with `inputLabel` (gate-12).
- [ ] 8. **Listing/detail UI** — verdict chip (`passed|warnings|failed|not yet validated`) on document listing + detail, findings panel with localised messages, `suggestedAction: "ocr"` findings cross-link to the OCR flow. English i18n keys, NL translations.
- [ ] 9. **Unit tests** — `tests/unit/Service/DocumentValidationServiceTest.php`: each check family fires + passes, severity mapping, profile fallback, threshold config, verdict aggregation (any failed → failed; else any warning → warnings; else passed), no-write purity.
- [ ] 10. **Newman integration tests** — on-demand endpoint happy/404, blocking 422 shape on upload with an opted-in blocking profile, warn-only default produces no 422, batch skip-and-continue.
- [ ] 11. **Playwright e2e** — admin configures a profile; operator uploads an encrypted PDF and sees the failed verdict + finding; scan-only PDF shows the OCR cross-link (gate-19 coverage for UI scenarios).
- [ ] 12. **Documentation** — `docs/features/document-validation.md` (check catalogue, profiles, blocking semantics), CHANGELOG entry, restore F-02 to *Beschikbaar* in the verify-passing PR.
- [ ] 13. **Quality + verification** — `composer check:strict`, hydra gates, live smoke test on the dev container (encrypted + scan-only fixtures through upload).
