## Tasks

- [x] 1. **Truthfulness fixes** — `docs/GOVERNMENT-FEATURES.md` F-02 now points at `features/document-validation.md` and describes the real check catalogue; the stale `docs/api/full-text-search.md` Solr doc is deleted (zero corresponding code; search is OR's domain per ADR-022), with the OR/NC unified-search pointer added to the validation feature doc.
- [x] 2. **`DocumentValidationService` — check catalogue** — all six checks implemented (`format-not-allowed`, `extension-mime-mismatch`, `file-unreadable`, `pdf-encrypted`, `text-layer-missing` with `suggestedAction: ocr`, `metadata-incomplete` naming the field). Pure computation: reads the file + record only, writes nothing. Text-layer threshold via `docudesk.validation.text_layer_min_chars_per_page` (default 32).
- [x] 3. **Validation profiles** — `docudesk.validation.profiles` JSON (per type: allowedMimes, requiredFields, per-check severity `off|warning|blocking`); shipped defaults warn-only with a sensible mime allowlist; unknown types fall back to `default`; profiles read at validation time.
- [x] 4. **Verdict as OR calculation** — `validationStatus` + `validationFindings` declared as `x-openregister-calculations` on `generatedDocument` in `docudesk_register.json` (backend `docudesk.validation`); register bumped to v5.8.0; event-listener fallback `ValidationRunner` (dispatched from `DocuDeskEventListener`) computes + stores the verdict via the existing metadata save path until OR's ADR-031 runtime ships. The listener contains no validation logic.
- [x] 5. **On-demand endpoint** — `POST /api/validation/validate` (`{fileId, documentType?}`), `#[NoAdminRequired]`, IDOR-safe file resolution (404 without disclosure), returns verdict + findings without persisting; route registered.
- [~] 6. **Pipeline gate** — DEFERRED: the upload/extract/batch/folder 422-blocking-gate wiring (skip-and-continue on batch/folder) touches `FileUploadService`/`BatchExtractionService`/`FolderBatchService` and is a focused follow-up. The verdict computation it gates on (severity → blocking → 422 shape) is fully built and unit-tested here; the gate is purely the call-site wiring + 422 response in the intake controllers. Default deployments are warn-only so no behaviour changes without this.
- [~] 7. **Admin settings UI** — DEFERRED with the admin-settings overhaul: the config key (`docudesk.validation.profiles`), severity semantics, and the "blocking active" summary are server-side built + tested; surfacing the per-type profile editor + severity selectors + banner needs the admin-settings block reorganisation (tracked alongside `docudesk-admin-settings-overhaul`). NcSelect-with-inputLabel requirement noted for that PR.
- [x] 8. **Listing/detail UI** — `ValidationFindingsPanel.vue` (verdict chip via `CnStatusBadge` + per-finding localised messages + `text-layer-missing` → OCR cross-link), `ValidationResultModal.vue` (isolated per gate-13), `validationService.js` (endpoint + `verdictColor`), and a **Validate** row action on My documents. English i18n source keys, NL translations. The on-demand panel is the primary verdict surface in v1.
- [x] 9. **Unit tests** — `tests/unit/Service/DocumentValidationServiceTest.php` (13 tests): each check family fires + passes, severity mapping, profile fallback, off-skip, threshold, verdict aggregation, default-never-blocks, no-content purity.
- [~] 10. **Newman integration tests** — DEFERRED: endpoint contract covered by PHPUnit (verdict/findings/no-persistence) + the ValidationController; a live Newman collection (happy/404/blocking-422) ships with the pipeline-gate follow-up (task 6) where the 422 path becomes reachable.
- [x] 11. **Playwright e2e** — `tests/e2e/spec-coverage/document-validation-checks.spec.ts` covers the two built UI scenarios (verdict/findings panel; OCR cross-link). Backend scenarios are requirement-level `@e2e exclude`; the admin-banner scenario carries a per-scenario deferral exclude. Gate-19 verified.
- [x] 12. **Documentation** — `docs/features/document-validation.md` rewritten from the aspirational stub to the real check catalogue, profiles, verdict/calculation, and endpoint; `docs/GOVERNMENT-FEATURES.md` F-02 updated; stale Solr doc removed.
- [~] 13. **Quality + verification** — Hydra gates: diff gate-clean (gate-16 spec-coverage, gate-19 e2e, gate-13 modal-isolation, gate-7 no-admin-idor all green; the 3 failing gates are pre-existing DossierController/SigningController baseline defects, unchanged here). PHPUnit 768/768 green, vitest validationService green, `npm run build` green. DEFERRED: live-stack smoke against encrypted + scan-only fixtures (needs dev container).
