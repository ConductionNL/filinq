## Tasks

- [ ] 1. **Truthfulness fix (immediate)** — edit `docs/GOVERNMENT-FEATURES.md` row F-07: status *Beschikbaar* → *Gepland (in ontwikkeling)* with a pointer to this change. This task is independent of the build and ships first.
- [ ] 2. **`DocumentComparisonService` — subject resolution** — resolve `{fileId, versionTimestamp?}` via `IRootFolder::getUserFolder()` (+ `IVersionManager` for versions); 404 on unknown/inaccessible file, 404 on unknown version, 422 with machine-readable reason when `files_versions` is disabled and a version subject is requested.
- [ ] 3. **`DocumentComparisonService` — diff engine** — text extraction via existing `DocumentTextExtractor`, whitespace normalisation, word-level LCS diff coalesced into hunks (`unchanged|added|removed|changed` with left/right offsets). Size cap via `docudesk.comparison.max_text_bytes` (default 5 MB) → 413. Decide in apply whether to promote `sebastian/diff` or implement LCS in-service.
- [ ] 4. **Redaction annotation** — map inserted spans to `EntityRelation` rows via the anonymisation result's replacement-key mapping (REQ-ANON-06), fall back to entity-value matching against `Entity` canonical values; attach `redaction: {entityId, entityType, matchedBy}` per hunk. OR mappers resolved lazily per REQ-ANON-05 (graceful degradation to plain diff when OR is unavailable).
- [ ] 5. **Completeness signal** — compute `unredactedEntities[]` (non-skip-flagged `EntityRelation` rows for the source file with zero matching change hunks), returned by entity ID + canonical name, never literal document text.
- [ ] 6. **`ComparisonController` + route** — `POST /api/comparison/compare`, `#[NoAdminRequired]`, per-subject access guards (gate-7 no-admin-idor), registered in `appinfo/routes.php` (gate-14 route-reachability), response shape per spec.
- [ ] 7. **Unit tests** — `tests/unit/Service/DocumentComparisonServiceTest.php`: version resolution, access denial, identical-documents zero-hunks, structured hunk shape, cross-format whitespace normalisation, key-based + value-based redaction annotation, completeness signal incl. skip-flagged exclusion, size cap, OR-unavailable degradation.
- [ ] 8. **Comparison UI** — side-by-side view (dedicated route), "Compare…" entry on document detail, pre-wired original-vs-anonymised entry on anonymisation results, file + version pickers, synchronized scroll, hunk highlighting, redaction badges, `unredactedEntities` advisory panel, cross-format notice. NL + EN strings (English i18n keys).
- [ ] 9. **Playwright e2e** — UI flow: open comparison from document detail, pick versions, see highlighted hunks and redaction badges (gate-19 coverage for the UI scenarios in the spec delta).
- [ ] 10. **Newman integration tests** — `tests/integration/`: compare-two-versions, compare-two-files, 404 inaccessible subject, 413 oversize, original-vs-anonymised annotated response, no-persistence assertion (object counts unchanged).
- [ ] 11. **Documentation** — `docs/features/document-comparison.md` (user flow + API), CHANGELOG entry, restore `docs/GOVERNMENT-FEATURES.md` F-07 to *Beschikbaar* in the verify-passing PR.
- [ ] 12. **Quality + verification** — `composer check:strict`, hydra gates, live smoke test against the dev container (compare an anonymised pair seeded via the anonymise pipeline).
