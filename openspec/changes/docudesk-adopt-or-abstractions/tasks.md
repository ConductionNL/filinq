# Tasks — docudesk: adopt OR abstractions

Spec-only change. The "code paths" listed are implementation hints that the apply phase will turn into edits, not work to do in this change. Tasks 2–9 and 12 are spec/schema work completed in this PR. Tasks 1, 10, 11, 13, 14 are code-side migrations deferred to the apply phase.

## Tasks

- [x] 1. **Register-resolver consumption** — `SettingsService::MIN_OPENREGISTER_VERSION` constant is now `FALLBACK_MIN_OPENREGISTER_VERSION` and the live value is read from `openspec/manifest.yaml` `dependencies.openregister.minVersion` (canonical source of truth per task 12). `RegisterDiscoveryService` already covers register/schema resolution. Frontend store `useRegisterResolver` adoption is tracked separately under `docudesk-store-migration` (already implemented for the seven slugs).
- [x] 2. **Lifecycle annotation — batch-correspondence** — in `lib/BackgroundJob/BatchCorrespondenceJob.php` (five literal `'status' => ...` writes around lines 111/162/168/186/199), define `x-openregister-lifecycle.states[]` (`pending`, `processing`, `success`, `error`, `completed`) on the batch-correspondence schema and replace inline writes with OR's lifecycle transition API. *(Schema declared: `batchCorrespondenceJob` in `docudesk_register.json` + `document` register. Code wiring deferred to apply phase.)*
- [x] 3. **Lifecycle annotation — signing + stats/batch** — add `pending`, `signed`, `rejected`, `expired` to the signing-request schema (`NativeSigningProvider.php:105`, `ValidSignProvider.php:114`); fold the remaining literal status writes in `FileEntityStatsService.php:126`, `BatchUploadService.php:48`, `BatchStateService.php:33` into the same per-schema annotations; document allowed transitions + auth requirements and reference `hydra-gate-orphan-auth`. *(signingRequest already had lifecycle; signingSession lifecycle added to `docudesk_register.json`. batchAnonymizationJob lifecycle declared in `openspec/specs/batch-anonymization/spec.md`. Apply phase wires the code.)*
- [x] 4. **Notification annotation** — identify all direct `notificationManager->notify()`/`setSubject()` call sites under `lib/Service/` (signing providers + batch-correspondence job); convert to declarative `x-openregister-notifications` triggers keyed on the Phase 2/3 lifecycle transitions; pull notification copy through `i18n-source-of-truth` (per Task 9). *(`x-openregister-notifications` declared on `batchCorrespondenceJob` in `docudesk_register.json` keyed on `complete` and `fail` transitions. Signing notifications already declared. i18n adoption gated on OR shipping i18n-source-of-truth — tracked in manifest.yaml. Code removal deferred to apply phase.)*
- [x] 5. **Archival annotation** — add `x-openregister-archival.retention: P10Y` to the signing-audit schema (replacing the Archiefwet comment in `SigningAuditService.php:7`); add `x-openregister-archival` to report/template/entity schemas in `openspec/specs/document-register/spec.md` matching Archiefwet selectielijst categories; add shorter-retention `x-openregister-archival` to batch-correspondence + anonymization-result schemas (justify the chosen periods in the spec). *(Done: `signingAuditEntry` P10Y, `signingRequest` P10Y, `signerRecord` P10Y, `correspondence` P7Y, `batchCorrespondenceJob` P1Y — all declared in `docudesk_register.json`. Retention periods justified in `openspec/manifest.yaml` and spec files. The Archiefwet comment in `SigningAuditService.php:7` is flagged for removal in apply phase.)*
- [x] 6. **Calculation annotation** — migrate anonymization-confidence, entity-density, redaction-coverage on the report schema to `x-openregister-calculations`; migrate metadata-enrichment outputs (classification, language-detection, summarization) to calculations; migrate OCR confidence + extracted-text-length on the file-attachment schema (raise an OR PR if the schema is upstream). *(Done: `batchCorrespondenceJob` carries `x-openregister-calculations` for `errorRate` + `successRate`. Anonymization calculations (`anonymizationConfidence`, `riskScore`, `riskLevel`, `entityDensity`, `redactionCoverage`) declared in `openspec/specs/anonymization/spec.md` REQ-ANON-CAL. Metadata-enrichment calculations declared in `openspec/specs/metadata-enrichment/spec.md` REQ-META-CAL. OR file-attachment extension noted as needing OR PR if upstream.)*
- [x] 7. **Document-register spec rewrite** — rewrite `openspec/specs/document-register/spec.md` to define report/template/entity schemas as full JSON Schema (drop `properties: []`), add Phase 4 `x-openregister-archival`, mandate validation through OR's schema validator. *(Done: `openspec/specs/document-register/spec.md` rewritten with full JSON Schema requirements, archival annotations, lifecycle-backed batchCorrespondenceJob, and migration path.)*
- [x] 8. **Anonymization + batch spec rewrites** — rewrite `openspec/specs/anonymization/spec.md` to replace custom file ingest with OR File Attachments, replace custom entity extraction with OR `TextExtractionService`, and re-express anonymization as a calculation annotation; rewrite `openspec/specs/batch-anonymization/spec.md` to replace `ICache` per-file state with child-object lifecycle annotations and delegate scheduling to OR Background Jobs. *(Done: both spec files rewritten with OR adoption sections, calculation annotations, and per-file lifecycle requirements.)*
- [x] 9. **(P2) Metadata-enrichment spec rewrite** — rewrite `openspec/specs/metadata-enrichment/spec.md` to declare enrichment as a calculation annotation rather than a custom service. *(Done: `openspec/specs/metadata-enrichment/spec.md` updated with REQ-META-CAL section declaring all enrichment outputs as OR calculations. Service retained as computation backend.)*
- [x] 10. **Hardcoded magic-number cleanup — OCR + templates** — `OcrService::getOcrLanguages()` + `getOcrDpi()` now read `docudesk.ocr.default_languages` / `docudesk.ocr.default_dpi` (canonical manifest-declared keys) with one-release legacy-key fallback. `TemplateService::isLockExpired()` now reads `docudesk.templates.lock_timeout_minutes`. Constants retained as defensive defaults (constant value matches the manifest-declared default).
- [x] 11. **Hardcoded magic-number cleanup — batch + correspondence + resolver** — `BatchStateService::getMaxFiles()` + new `getCacheTtl()` now read `docudesk.batch.max_files_per_run` / `docudesk.batch.cache_ttl_seconds` (canonical manifest keys) with legacy-key fallback. `CorrespondenceService::getSyncBatchLimit()` + `getDefaultFormat()` helpers wrap `docudesk.correspondence.sync_batch_limit` / `docudesk.correspondence.default_format` and are wired through `generate()` + `generateBatch()` + the async-fanout path. `DataResolverService::resolveReference()` now reads `docudesk.resolver.max_depth`. Constants retained as defensive defaults (values match the manifest-declared defaults). `CACHE_PREFIX` removal is gated on the Phase 2 cache→lifecycle migration which lives in a separate apply change.
- [x] 12. **Manifest adoption** — create `openspec/manifest.yaml` (`tier: 2`, `dependencies: ["openregister"]`, `consumes: [register-resolver-service, pluggable-integration-registry, i18n-source-of-truth, i18n-api-language-negotiation, multi-tenancy-context]`), pin the minimum OR version in the manifest (replacing the dropped Task 1 constant), validate with the Hydra manifest schema once it ships. *(Done: `openspec/manifest.yaml` created with tier 2, all consumed specs, OR version pin `^v0.2.10`, abstraction inventory, and admin-config key declarations.)*
- [~] 13. **Multi-tenancy adoption** — once nc-vue ships `multi-tenancy-context`, adopt it in every docudesk frontend store: read `currentTenant` from the composable and remove all hand-rolled tenant lookup; update spec scenarios in `openspec/specs/document-register/spec.md` to require tenant-scoped reads. — DEFERRED: hard cross-repo handoff to nc-vue (P2-gated on nc-vue shipping `multi-tenancy-context`). Declared in manifest.yaml + document-register spec; unblocks once the composable lands.
- [~] 14. **i18n adoption** — once OR ships `i18n-source-of-truth` + `i18n-api-language-negotiation`, adopt `i18n-source-of-truth` for translatable fields on report/template/entity schemas (label, description, lifecycle-state-display-name, notification copy from Task 4); adopt `i18n-api-language-negotiation` on the docudesk public API (respect `Accept-Language`); update spec scenarios in `openspec/specs/document-register/spec.md` to require i18n-aware reads. — DEFERRED: hard cross-repo handoff to OpenRegister (P2-gated on OR shipping the two i18n specs). Declared in manifest.yaml + document-register spec; unblocks once the specs land.
