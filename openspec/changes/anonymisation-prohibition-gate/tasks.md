## Tasks

- [x] 1. **PolicyMatchService availability** — confirm `PolicyMatchService` exists in `lib/Service/`; if `entity-publication-policies` apply hasn't landed it, scaffold from that change's spec (minimum: `matchProhibition(entityText, entityType, resolvedIdentifiers): ?MatchedRule`); cache active `publicationProhibition` records in memory and subscribe to OpenRegister object-changed events for invalidation; separate the prohibition cache from the standing-consent cache so each can land independently.
- [x] 2. **PolicyMatchService unit tests** — unit-test `matchProhibition` on its own: high-confidence match, low-confidence match, no match, multi-rule deterministic precedence, time-bounded rules honoured, prohibition portion of cache populated correctly.
- [x] 3. **Prohibition gate in AnonymizationService** — in `AnonymizationService::anonymizeDocument` (and the batch equivalent), before forwarding to OpenRegister: resolve detected entities via `EntityRelationMapper::findEntitiesForFile`, call `PolicyMatchService::matchProhibition` on each, collect `(ruleId, entityId, confidence, ruleName, entityName)`.
- [x] 4. **422 contract — missing high-confidence matches** — for each high-confidence (≥ threshold) match, verify the entity is in the request payload's `entities[]`; if any are missing, fail with HTTP 422 and body `{error, missingProhibitionMatches: [{entityId, entityName, ruleId, ruleName, confidence}]}`; `entityName` MUST come from the OpenRegister `Entity` record canonical name (via `EntityMapper::find($entityId)` or equivalent), NOT the literal detected text, NOT the rule's `primaryName`.
- [x] 5. **Threshold config** — read the high-confidence threshold from `docudesk.prohibition.high_confidence_threshold` (default 0.85) via the existing `IAppConfig` pattern used by `EntityDetectionService`; runtime config changes propagate without restart.
- [x] 6. **`acknowledgedOverrides` validation** — accept an optional top-level `acknowledgedOverrides` array on the anonymise payload (`{ruleId, entityId, reason?}`); valid override: `(ruleId, entityId)` must match an active prohibition match with confidence < threshold → "released"; high-confidence-match override: reject with 422 body containing a `rejectedOverrides` array alongside any `missingProhibitionMatches`; non-matching combination: silently ignore.
- [x] 7. **Logging** — on 422 firing, log `ruleId`, `entityId`, `fileId`; do NOT log the literal detected text; canonical entity name MAY appear in logs only because it's already in the response payload.
- [x] 8. **In-place wording fix — entity-publication-policies (parent spec)** — edit `openspec/changes/entity-publication-policies/specs/entity-publication-policies/spec.md` "Out-of-scope behaviors MUST remain unchanged" → "Generic anonymisation" bullet: replace with *"Generic anonymisation flows (file sanitisation not destined for publication) — these do not invoke `createConsentRequest()` and therefore do not create `publicationConsent` records or pre-empt any workflow. They MAY read the `publicationProhibition` list as a data source for safety checks (e.g. the prohibition gate specced in `anonymisation-prohibition-gate`); read access to a register is not workflow integration."*
- [x] 9. **In-place wording fix — consent-management delta** — edit `openspec/changes/entity-publication-policies/specs/consent-management/spec.md` trigger-boundary preamble paragraph that asserts generic anonymisation does not interact with this policy layer; apply the same workflow-integration vs read-only-access tightening; run `openspec validate entity-publication-policies` and confirm it still validates clean.
- [x] 10. **Unit tests — AnonymizationService gate** — `tests/unit/Service/AnonymizationServiceTest.php`: gate fires on missing high-confidence; gate passes when all high-confidence are included; gate ignores low-confidence by default; override releases low-confidence; override rejects high-confidence.
- [x] 11. **Unit tests — controller** — `tests/unit/Controller/AnonymizationControllerTest.php`: 422 response shape; `acknowledgedOverrides` accepted on first request (no special retry flag); rejected-overrides body shape.
- [ ] 12. **Integration tests — Newman** — anonymise gate fires 422 with the documented body when a high-confidence prohibition is missing; anonymise succeeds when all high-confidence prohibitions are included; `acknowledgedOverrides` releases a low-confidence match and rejects an override on a high-confidence match.
- [x] 13. **Documentation** — add `docs/features/anonymisation-prohibition-gate.md` (or extend `docs/features/publication-consent-process.md`) describing the gate, the override mechanism, the 422 response shape, the threshold config; CHANGELOG "Added": prohibition gate on anonymise endpoint; CHANGELOG "Behavior changes": anonymise may now respond HTTP 422 when prohibition-listed entities are missing (existing callers with no prohibitions see no change).
- [x] 14. **Quality + verification** — `composer check:strict` clean (fix any pre-existing issues in touched files); manual smoke: configure a prohibition rule, extract a doc containing the entity, verify `prohibitionMatch` in response, submit anonymise without the entity → 422, submit again with the entity → 200; `openspec validate anonymisation-prohibition-gate` clean; `openspec validate entity-publication-policies` clean after the in-place wording fix.
