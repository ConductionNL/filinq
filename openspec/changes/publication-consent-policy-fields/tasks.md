## Tasks

- [x] 1. **`scope` discriminator** — extend `publicationConsent` in `lib/Settings/docudesk_register.json` with `scope` (enum `document` / `entity`, default `document`); existing records load as `document` automatically; verify schema imports cleanly via `composer test:unit`.
- [x] 2. **Entity-scope field set** — extend the schema with `matchRules` (same enum as the sibling prohibition: exact/normalized/bsn/kvk), `validFrom`, `validUntil`, `active`, `consentMethod` (enum: paper / digital_signature / verbal_recorded / opt_in_form), `consentDocument` (file ref), `consentScope`; none in the schema's top-level `required` — service-level enforcement gates them by `scope`.
- [x] 3. **`policyMatch` polymorphic reference** — add the new `policyMatch` property: `type: "object"`, `oneOf` with `$ref` to `publicationProhibition` and to `publicationConsent` (polymorphic self-reference for standing consents), `objectConfiguration.handling: "related-object"`, OPTIONAL; confirm `consentStatus` enum is NOT changed (existing values cover all outcomes).
- [x] 4. **Seed data** — add four realistic standing-consent seed objects as `publicationConsent` records with `scope: "entity"`: mayor blanket consent, organisation signed opt-in, council member, recorded verbal consent; `@self` envelope per ADR-013; `consentStatus: "consent_given"`, `consentMethod` populated, `validFrom` / `validUntil` set, `active: true`, `legalBasis` populated; verify seed import is idempotent.
- [x] 5. **Service-level scope validation** — implement at write time in `ConsentService`: reject `scope: "document"` writes missing `documentId`; reject `scope: "entity"` writes that include `documentId`; reject `scope: "entity"` writes missing `matchRules` or `consentMethod`; reject `policyMatch` on `scope: "entity"` records; reject `policyMatch` referents whose own `scope` is not `"entity"`.
- [x] 6. **Standing-consent matching path** — extend `ConsentService::createConsentRequest()` to consult `PolicyMatchService` (from sibling) for standing-consent matches AFTER the prohibition check; on standing-consent match: create a `scope: "document"` `publicationConsent` with `consentStatus: "consent_given"`, `notificationStatus: "skipped"`, `publicationDecision: "publish_with_consent"`, `objectionDeadline: null`, `policyMatch` referencing the standing-consent record.
- [x] 7. **Status-transition blocks on policy-matched records** — any attempted transition of `consentStatus` to a different terminal value is rejected; allowed: re-pointing `policyMatch` if the underlying rule is replaced (still pointing at a permitted referent type), and recording an override-up on standing-consent matches (`publicationDecision: "anonymize"` while `consentStatus` stays `consent_given` and `policyMatch` is preserved).
- [x] 8. **Override-up flow** — when the user flips the per-entity toggle ON for a standing-consent match: record as a status transition — emit an audit event and update `publicationDecision: "anonymize"` while keeping `consentStatus: "consent_given"` and preserving `policyMatch`.
- [x] 9. **Consent service unit tests** — four detection-time outcomes: no match → WOO; prohibition match → anonymized + policyMatch → prohibition; standing-consent match → consent_given + policyMatch → standing consent; both match → prohibition wins (audit logs both rule UUIDs); scope-validation rules at write time (the four corners: scope × valid/invalid required-set); override-up flow.
- [x] 10. **RBAC service-level enforcement on `scope: "entity"` writes** — writing a `scope: "entity"` record requires membership in `docudesk-standing-consent-admins`; enforced before save (not at schema level); smoke-test that a consent-officer can POST `scope: "document"` records but is rejected when attempting `scope: "entity"`.
- [x] 11. **Admin UI — Standing Publication Consents page** — new CnIndexPage view; filtered to `scope: "entity"`; list/edit/expire/revoke; create-form requires `consentMethod` and surfaces a non-blocking warning when `validUntil` is blank; encourages stable identifiers (BSN/KvK); CnFormDialog + CnDataTable per ADR-012.
- [x] 12. **Admin UI — Consent Workflow `scope: "document"` filter + toggle behaviour** — apply hard filter `scope: "document"` on the existing Consent Workflow page; per-entity toggle behaviour keyed off `policyMatch` referent type: `policyMatch → publicationConsent` (scope=entity) → toggle OFF (default), enabled, user may flip ON for override-up; `policyMatch: null` → existing UX based on `consentStatus`.
- [ ] 13. **Browser + integration tests** — Playwright: defaulted-off-overridable toggle for standing-consent matches; Standing Publication Consents page lists only `scope: "entity"` records; create form requires `consentMethod`; standing consents and prohibitions never appear on the wrong page; Newman: CRUD for `scope: "entity"` `publicationConsent`; four detection-time outcomes; retroactive non-application of standing consents (create in-flight `scope: "document"` with `objection_received`; create matching standing consent; verify existing record stays as-is); schema-validation rejection for `policyMatch` pointing at a `scope: "document"` referent.
- [x] 14. **Documentation + quality** — extend `docs/features/publication-consent-process.md` with the standing-consent flavour, `scope` discriminator, `policyMatch` polymorphic reference, Standing Publication Consents admin page, override-up flow; CHANGELOG "Added": `scope` discriminator + entity-scope field set on `publicationConsent` + `policyMatch` polymorphic reference + "Standing Publication Consents" admin page; "Behavior changes": standing-consent pre-emption inside publication-clearance flows (generic anonymisation unaffected); `composer check:strict` clean (fix pre-existing issues in touched files); `openspec validate publication-consent-policy-fields` clean.
