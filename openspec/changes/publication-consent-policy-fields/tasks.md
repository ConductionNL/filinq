## Tasks

- [x] 1. **`scope` discriminator** — added `scope` property to `publicationConsent` in `lib/Settings/docudesk_register.json` (enum `document` / `entity`, default `document`, facetable). Existing rows load as `document` automatically because the property is not in `required[]` and the default applies on read. JSON validated via `python3 json.load`.
- [x] 2. **Entity-scope field set** — added `matchRules` (with the same `exact / normalized / bsn / kvk` enum as the sibling `publicationProhibition`), `validFrom`, `validUntil`, `active`, `consentMethod` (enum `paper / digital_signature / verbal_recorded / opt_in_form`), `consentDocument` (file URI), and `consentScope` (free-text scope limitation). None added to the schema's top-level `required[]` — the description on each property names the service-level scope-gated enforcement.
- [x] 3. **`policyMatch` polymorphic reference** — added `policyMatch` as a `string` URI carrying `x-openregister-handling: related-object`. The polymorphic referent type (publicationProhibition vs publicationConsent-with-scope-entity) is enforced at the service layer (task 5) because OR's `$ref` polymorphism via `oneOf` is not honoured by the current schema loader; the description documents the polymorphic contract. The `consentStatus` enum is NOT changed (existing values cover all outcomes).
- [~] 4. **Seed data** — four realistic standing-consent seed objects (mayor blanket / organisation signed opt-in / council member / recorded verbal consent) as `publicationConsent` records with `scope: "entity"` — DEFERRED: live-environment seed verification depends on the register-import repair step landing in a Nextcloud container; the schema now accepts the rows so seeds can be added in a follow-up live-env capture.
- [x] 5. **Service-level scope validation** — added `lib/Service/ConsentScopeValidator.php` (stateless service) wired into `ConsentService::createConsentRequest()` immediately before `saveObject`. The validator throws `InvalidArgumentException` (surfaced as HTTP 400) when: `scope=document` is missing `documentId`; `scope=entity` carries `documentId`; `scope=entity` is missing `matchRules`/`consentMethod`; `scope=entity` carries `policyMatch`; unknown scope value. Polymorphic-referent type check (policyMatch must point at publicationProhibition or scope=entity publicationConsent) is service-level because it requires an OR lookup and is enforced in the standing-consent match path (task 6 / line 495 of ConsentService).
- [x] 6. **Standing-consent matching path** — already implemented in `ConsentService::createConsentRequest()`: `policyMatcher->match()` runs after the prohibition check and the result's `kind` discriminates between `KIND_PROHIBITION` (line 197) and `KIND_STANDING_CONSENT` (line 495). On standing-consent match the resulting `scope=document` publicationConsent carries `policyMatch` referencing the standing-consent record UUID.
- [~] 7. **Status-transition blocks on policy-matched records** — DEFERRED: requires extending `ConsentUpdateHandler` with a policyMatch-aware transition guard; the scope validator already covers the create-side contract. The transition guard sits alongside the override-up flow (task 8) and ships as one PR with the Vue per-entity-toggle wiring.
- [~] 8. **Override-up flow** — DEFERRED with task 7: the override audit event + `publicationDecision: "anonymize"` update needs the per-entity toggle UI in the Consent Workflow page; tracked alongside task 12.
- [x] 9. **Consent service unit tests** — `tests/unit/Service/ConsentScopeValidatorTest.php` covers the four-corner scope-validation contract (document missing documentId, entity carrying documentId, entity missing matchRules, entity carrying empty matchRules, unknown match-rule type, entity missing consentMethod, entity carrying policyMatch, valid document, valid entity, unknown scope). Detection-time outcomes (prohibition / standing-consent / no-match / both) are covered by the pre-existing `PolicyMatchServiceTest` + `AnonymizationServiceProhibitionTest` + `ConsentServiceTest`. Override-up flow tests ship alongside the deferred tasks 7-8.
- [~] 10. **RBAC service-level enforcement on `scope: "entity"` writes** — DEFERRED with tasks 7-8: requires a `docudesk-standing-consent-admins` group check before save; the schema-side `authorization` block on `publicationConsent` can express this directly per ADR-005 once the standing-consent group is provisioned.
- [~] 11. **Admin UI — Standing Publication Consents page** — DEFERRED with task 7-8: ships alongside the override-up toggle so the UI surfaces are designed against the same store/composable refactor.
- [~] 12. **Admin UI — Consent Workflow `scope: "document"` filter + toggle behaviour** — DEFERRED with tasks 7-8.
- [~] 13. **Browser + integration tests** — DEFERRED: requires the deferred UI surfaces (tasks 11-12); the service-layer contract has unit-test coverage (task 9).
- [~] 14. **Documentation + quality** — DEFERRED: `composer check:strict` requires `vendor/` (no composer install in this worktree); doc additions ship with the UI surfaces. Schema + service-validator are in place; openspec validation runs against the dev container in a follow-up live-env PR.
