## Tasks

- [ ] 1. **`prohibitionMatch` per consolidated entity** — update the `GET /api/anonymization/batch/{batchId}/entities` handler to call `PolicyMatchService::matchProhibition` for each consolidated entity; attach `prohibitionMatch` per entity with the same shape as extract (`null` or `{ruleId, ruleName, highConfidence}`).
- [ ] 2. **`highestConfidence` for threshold check** — use the existing `highestConfidence` value already returned by the endpoint when computing `prohibitionMatch.highConfidence`; threshold reads from `docudesk.prohibition.high_confidence_threshold` (default 0.85), inclusive at the boundary, same key + same semantics as the gate.
- [ ] 3. **Dossier resolver** — resolve the dossier(s) the batch's files belong to; read each dossier's `bases[]` and union them, deduplicated; attach `suggestedBases` (array of UUIDs) per entity.
- [ ] 4. **Resolver edge cases** — files not in any dossier → `suggestedBases: []`; dossier with empty `bases[]` → contributes nothing; files spread across multiple dossiers → union (not intersection), deduplicated.
- [ ] 5. **Resolver unit tests** — `tests/unit/Service/BasesResolverTest.php` (or co-located): dossier-bound batch, orphan batch, multi-dossier batch, empty-dossier-bases batch.
- [ ] 6. **Endpoint unit tests** — `prohibitionMatch: null` when no rules; right object when rule matches; `highConfidence` reflects threshold correctly at boundary; backward compatibility — pre-change clients reading only `type`/`value`/`highestConfidence`/`fileCount`/`included` continue to work; the response is a strict superset of the pre-change shape.
- [ ] 7. **Integration tests — Newman** — call consolidated-entities for batches that cover the cases above (no prohibitions, high-confidence match, low-confidence match, dossier-bound, orphan, multi-dossier); assert response shape and field values.
- [ ] 8. **Documentation** — update `docs/features/anonymization.md` review-UI section: `prohibitionMatch` + `suggestedBases` are now exposed on the consolidated-entities endpoint; CHANGELOG "Added": `prohibitionMatch` + `suggestedBases` on entity-listing responses.
- [ ] 9. **Quality + verification** — `composer check:strict` clean (fix any pre-existing issues in touched files); `openspec validate anonymisation-entity-review-prohibition-hints` clean.
