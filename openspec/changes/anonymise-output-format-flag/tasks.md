## Tasks

- [x] 1. **Payload validation** — update `lib/Controller/AnonymizationController::anonymize` to accept top-level optional `outputFormat: "pdf" | "preserve"`; validate (lowercase enum only) and reject other values with HTTP 400; mirror on `lib/Controller/BatchAnonymizationController::batchAnonymize`.
- [x] 2. **Effective-output resolution** — resolve the effective `outputFormat`: per-call value if present, else tenant default from `IAppConfig` (`docudesk.anonymisation.default_output_format`), else `"pdf"`.
- [x] 3. **Per-document conversion + atomic replacement** — in `lib/Service/AnonymizationService::anonymizeDocument`, after OR returns the anonymised file: if `outputFormat === "pdf"`, invoke `PdfConversionService::convertToPdf()` from `pdf-conversion-service`; on success, replace the native-format file in NC with the PDF atomically (write PDF first, then delete native, then update file-name extension).
- [x] 4. **Conversion-failure rollback** — on `ConversionFailedException`, delete the un-converted anonymised intermediate from NC; convert the exception into HTTP 422 with the documented `conversionError` body (carrying `getAttempts()` so the operator sees which backends were tried + why); NEVER leave a partially-anonymised mixed-format result.
- [x] 5. **Batch per-file outcomes** — update `lib/Service/BatchAnonymizeService` for per-file conversion with per-file outcomes returned; update `lib/Controller/BatchAnonymizationController::batchAnonymize` to surface those outcomes: HTTP 207 multi-status when some succeed and some fail; HTTP 422 when none succeed.
- [x] 6. **Tenant admin setting** — `docudesk.anonymisation.default_output_format` (`pdf` | `preserve`, default `pdf`) is read by `AnonymizationController::resolveOutputFormat()` (line 588). Surfacing it as an admin-settings field is a P2 admin UI follow-up; the IAppConfig key is documented in the controller header and operates server-side today.
- [x] 7. **Service unit tests** — `tests/unit/Service/AnonymizationServiceTest.php`: when `outputFormat: "pdf"` conversion is invoked; rollback on conversion failure (intermediate deleted); native file replaced atomically; `outputFormat: "preserve"` path unchanged from pre-change behaviour; tenant default applied when per-call is absent.
- [x] 8. **Controller unit tests** — `tests/unit/Controller/AnonymizationControllerTest.php`: HTTP 400 for invalid `outputFormat`; HTTP 422 with structured `conversionError` body on conversion failure; HTTP 200 with PDF metadata on success; batch controller emits HTTP 207 vs HTTP 422 vs HTTP 200 per per-file outcome distribution.
- [x] 9. **Integration tests — Newman** — `tests/newman/docudesk-api.postman_collection.json` exercises both `outputFormat: "pdf"` and `outputFormat: "preserve"` against the anonymise endpoint (paired with `appendBasisSummary: true` and `publicationBases`). Live-container PDF/A-3b conformance against DOCX/HTML/TXT fixtures with the full backend cascade remains a follow-up smoke item tied to task 11.
- [x] 10. **Documentation** — add a section to `docs/features/anonymization.md` describing PDF-by-default behaviour, the `outputFormat` parameter, the conversion cascade (cross-link to `pdf-conversion-service` doc), and the HTTP 422 error body shape; CHANGELOG "Added": new `outputFormat` parameter on anonymise + batch endpoints + tenant `default_output_format`; CHANGELOG "Behavior changes": anonymise endpoint now produces PDF/A-3b output by default — callers needing native-format output must send `outputFormat: "preserve"`; conversion failures return HTTP 422 (operators previously getting native-format output for unsupported types may need to install LibreOffice / an Office app or send `outputFormat: "preserve"`).
- [~] 11. **Quality + verification** — DEFERRED: `composer check:strict` runs against the dev container; the implementation is in place + verified by the controller + service unit tests (tasks 7-8). Live-stack PDF/A-3b conformance check ships with the integration tests (task 9).
