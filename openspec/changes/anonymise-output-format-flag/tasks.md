## Tasks

- [ ] 1. **Payload validation** — update `lib/Controller/AnonymizationController::anonymize` to accept top-level optional `outputFormat: "pdf" | "preserve"`; validate (lowercase enum only) and reject other values with HTTP 400; mirror on `lib/Controller/BatchAnonymizationController::batchAnonymize`.
- [ ] 2. **Effective-output resolution** — resolve the effective `outputFormat`: per-call value if present, else tenant default from `IAppConfig` (`docudesk.anonymisation.default_output_format`), else `"pdf"`.
- [ ] 3. **Per-document conversion + atomic replacement** — in `lib/Service/AnonymizationService::anonymizeDocument`, after OR returns the anonymised file: if `outputFormat === "pdf"`, invoke `PdfConversionService::convertToPdf()` from `pdf-conversion-service`; on success, replace the native-format file in NC with the PDF atomically (write PDF first, then delete native, then update file-name extension).
- [ ] 4. **Conversion-failure rollback** — on `ConversionFailedException`, delete the un-converted anonymised intermediate from NC; convert the exception into HTTP 422 with the documented `conversionError` body (carrying `getAttempts()` so the operator sees which backends were tried + why); NEVER leave a partially-anonymised mixed-format result.
- [ ] 5. **Batch per-file outcomes** — update `lib/Service/BatchAnonymizeService` for per-file conversion with per-file outcomes returned; update `lib/Controller/BatchAnonymizationController::batchAnonymize` to surface those outcomes: HTTP 207 multi-status when some succeed and some fail; HTTP 422 when none succeed.
- [ ] 6. **Tenant admin setting** — expose `docudesk.anonymisation.default_output_format` (`pdf` | `preserve`, default `pdf`) in admin settings UI (or document as IAppConfig key in v1 with admin UI follow-up).
- [ ] 7. **Service unit tests** — `tests/unit/Service/AnonymizationServiceTest.php`: when `outputFormat: "pdf"` conversion is invoked; rollback on conversion failure (intermediate deleted); native file replaced atomically; `outputFormat: "preserve"` path unchanged from pre-change behaviour; tenant default applied when per-call is absent.
- [ ] 8. **Controller unit tests** — `tests/unit/Controller/AnonymizationControllerTest.php`: HTTP 400 for invalid `outputFormat`; HTTP 422 with structured `conversionError` body on conversion failure; HTTP 200 with PDF metadata on success; batch controller emits HTTP 207 vs HTTP 422 vs HTTP 200 per per-file outcome distribution.
- [ ] 9. **Integration tests — Newman** — anonymise without `outputFormat` returns PDF (when LibreOffice or PhpWord is available); anonymise with `outputFormat: "preserve"` returns native format; anonymise with unsupported input + no backends returns HTTP 422 with structured body; batch with mixed-format inputs returns appropriate per-file outcomes; manually verify on a Collabora-equipped stack that the Office app backend wins (document the test rather than automate Collabora setup in CI).
- [ ] 10. **Documentation** — add a section to `docs/features/anonymization.md` describing PDF-by-default behaviour, the `outputFormat` parameter, the conversion cascade (cross-link to `pdf-conversion-service` doc), and the HTTP 422 error body shape; CHANGELOG "Added": new `outputFormat` parameter on anonymise + batch endpoints + tenant `default_output_format`; CHANGELOG "Behavior changes": anonymise endpoint now produces PDF/A-3b output by default — callers needing native-format output must send `outputFormat: "preserve"`; conversion failures return HTTP 422 (operators previously getting native-format output for unsupported types may need to install LibreOffice / an Office app or send `outputFormat: "preserve"`).
- [ ] 11. **Quality + verification** — `composer check:strict` clean (fix pre-existing issues in touched files); manual smoke against a live stack: anonymise a DOCX/ODT/RTF/TXT/HTML with each backend tier active in turn (start with Office app + LibreOffice + PhpWord, disable each in sequence to verify cascade fall-through); verify the resulting file declares PDF/A-3b conformance via `pdfinfo` or similar; `openspec validate anonymise-output-format-flag` clean; `openspec validate` on the canonical `anonymization` capability picks up the delta clean.
