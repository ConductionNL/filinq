## Tasks

- [ ] 1. **Payload validation** — update `lib/Controller/AnonymizationController::anonymize` to accept optional `unredactedEntities[]` per design D2; each entry: `entityId` (int, required), `entityText` (string, required), `entityType` (enum, required), `publicationBases[]` (array of strings, required, non-empty), `contactEmail` (string, optional), `contactAddress` (string, optional); reject malformed input with HTTP 400.
- [ ] 2. **Prohibition gate (hard, any-confidence)** — before triggering the post-anonymise consent creation, run `PolicyMatchService::matchProhibition` for every `unredactedEntities[]` entry; if any match (any confidence — operator made an explicit decision), fail with HTTP 422; body lists offending entries with `{entityId, entityText, ruleId, ruleName}`; no `acknowledgedOverrides` accepted here (the operator must move the entity to `entities[]` to anonymise).
- [ ] 3. **Post-anonymise orchestration** — in `lib/Service/AnonymizationService::anonymizeDocument`, after the existing anonymise pipeline (entities + prohibition gate + bases passthrough) completes successfully, for each `unredactedEntities[]` entry call `ConsentService::createConsentRequest()` (per sibling `consent-create-idempotency-and-notes`); if the anonymise pipeline fails, no consents are created.
- [ ] 4. **`createdConsents[]` response field** — aggregate the per-entry result of `createConsentRequest()` into the response: one entry per `unredactedEntities[]` entry — either a "created" record (new publicationConsent UUID + status) or an "updated" record (existing UUID + status); the field is absent for callers that don't supply `unredactedEntities`.
- [ ] 5. **Batch endpoint integration** — `lib/Controller/BatchAnonymizationController::batchAnonymize` accepts the same `unredactedEntities[]` per file; `lib/Service/BatchAnonymizeService` does per-file consent creation; per-file 422 surfaced as HTTP 207 multi-status; HTTP 422 when none succeeded; HTTP 200 when all succeeded.
- [ ] 6. **Controller unit tests** — `tests/unit/Controller/AnonymizationControllerTest.php`: payload validation rejects malformed `unredactedEntities[]` (HTTP 400); empty `publicationBases[]` rejected; prohibition match → HTTP 422 with structured body listing offenders; success path returns HTTP 200 with `createdConsents[]` populated; backward-compat: omitting `unredactedEntities` produces no `createdConsents` field and behaviour matches pre-change exactly.
- [ ] 7. **Service unit tests** — `tests/unit/Service/AnonymizationServiceTest.php`: post-anonymise orchestration runs only after the existing pipeline succeeds; on anonymise-pipeline failure (gate, conversion, OR error), no consents created; per-entry call order matches the input array.
- [ ] 8. **Integration tests — Newman** — anonymise call with mixed `entities[]` + `unredactedEntities[]`: verify expected `publicationConsent` records exist after the call (one per unredacted entity); call with a prohibition-listed entity in `unredactedEntities[]`: HTTP 422 with structured body; re-submit with the entity moved to `entities[]`: HTTP 200; batch call with one prohibition-violating file: HTTP 207 with per-file outcomes.
- [ ] 9. **Documentation** — extend `docs/features/publication-consent-process.md` with the publication-clearance-via-anonymise flow: payload shape, prohibition-gate semantics (any-confidence hard 422), `createdConsents[]` response shape, batch multi-status semantics; CHANGELOG "Added": `unredactedEntities[]` on per-document and batch anonymise endpoints; `createdConsents[]` in response; "Behavior changes": anonymise may now respond HTTP 422 when prohibition-listed entities appear in `unredactedEntities[]`; HTTP 207 multi-status on batch when some files fail consent creation.
- [ ] 10. **Quality + verification** — `composer check:strict` clean (fix pre-existing issues in touched files); manual smoke against a live stack: anonymise call with mixed entities + unredactedEntities, verify expected publicationConsent records; verify 422 path with a prohibition-listed entity; verify batch HTTP 207 path; `openspec validate publication-clearance-anonymise-payload` clean.
