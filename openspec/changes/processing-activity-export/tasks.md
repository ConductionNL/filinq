## Tasks

- [ ] 1. **Aggregate DTO + `ProcessingActivityService`** — per activity category (`anonymisation`, `ocr`, `metadata-enrichment`, `signing`) over `[from, to]`: run count, document count, entity-type breakdown (from `Entity`/`EntityRelation` rows), legal-bases breakdown (from `EntityRelation.bases` with explicit `no-grondslag-recorded` bucket), backend identifier, retention references read from `x-openregister-archival` annotations in `docudesk_register.json` ("not declared" when absent). DTO is structurally aggregate-only (no field can carry entity values, document text, or file names). OR mappers resolved lazily (REQ-ANON-05 pattern); mapper-level counting, no full-object hydration.
- [ ] 2. **Range guard** — `docudesk.processing_activity.max_range_days` (default 366), request-time read; exceeding → 422.
- [ ] 3. **Controller-identity admin settings** — verwerkingsverantwoordelijke name, contact, FG/DPO contact stored in app config; settings section in DocuDesk admin UI (NcSelect/inputs with proper labels, gate-12).
- [ ] 4. **`ProcessingActivityController` + routes** — `GET /api/processing-activities` (JSON aggregate) and `GET /api/processing-activities/export?from&to&format=json|csv|pdf`; admin-only (no `#[NoAdminRequired]` — NC SecurityMiddleware default), routes registered (gate-14), semantic-auth consistent (gate-9).
- [ ] 5. **CSV serialisation** — one row per (activity category × entity type × base), UTF-8 with BOM for spreadsheet compatibility, header row with controller identity + period.
- [ ] 6. **PDF rendering** — `lib/Resources/templates/processing/verwerkingsregister.twig` rendered via existing `PdfService` (GrondslagenSummaryService pattern): Art. 30 header (controller identity, FG contact, period, generated-at), per-category sections, fixed declarations (local processing, no third-country transfer by DocuDesk). NL-only v1; EN deferred to `register-i18n`.
- [ ] 7. **Unit tests** — `tests/unit/Service/ProcessingActivityServiceTest.php`: category aggregation, entity-type + bases breakdowns, `no-grondslag-recorded` bucket, retention reference resolution incl. "not declared", range guard, OR-unavailable degradation, DTO contains no PII-capable fields (shape assertion).
- [ ] 8. **Newman integration tests** — aggregate endpoint shape, export in all three formats, 422 over-range, 401/403 for non-admin, no-literal-PII assertion on a seeded dataset (known entity value must not appear in any export byte stream).
- [ ] 9. **Admin UI export surface** — settings/compliance section with date-range picker + format selector + download; Playwright e2e for the admin flow (gate-19 coverage). English i18n keys, NL translations.
- [ ] 10. **Documentation** — `docs/features/processing-activity-export.md` (what the export contains, what it deliberately omits, Art. 30 mapping table, FG ownership note), CHANGELOG entry, consider adding an F-row to `docs/GOVERNMENT-FEATURES.md` (*Beschikbaar* only after verify).
- [ ] 11. **Quality + verification** — `composer check:strict`, hydra gates, live smoke test on the dev container: anonymise a seeded document, export the register, verify counts and absence of the seeded entity value.
