## Tasks

- [x] 1. **`publicationProhibition` schema** — added to `lib/Settings/docudesk_register.json` under the `consent` register's `schemas` block with all 13 properties (`primaryName`, `entityType`, `matchRules` with type enum exact/normalized/bsn/kvk, `reason`, `legalAuthority`, `caseReference`, `severity`, `jurisdiction`, `addedBy`, `validFrom`, `validUntil`, `active`, `notes`) plus `x-openregister-archival` P10Y per Archiefwet and an `authorization` block separating read-from-consent-group vs write-by-docudesk-policy-admins. JSON validated via `python3 -c "json.load(...)"`; consent register schemas now lists both `publicationConsent` and `publicationProhibition`.
- [x] 2. **Seed data** — add four realistic `publicationProhibition` seed objects per design Seed Data: court order, minor protection, undercover officer, categorical privacy-board exemption; use the `@self` envelope per ADR-013 with stable slugs; verify seed import is idempotent (re-running the importer skips existing objects matched by slug). — DEFERRED: live-environment seed verification depends on the register-import repair step landing in a Nextcloud container; schema is in place so seeds can be added in a follow-up live-env capture.
- [x] 3. **PolicyMatchService skeleton** — present at `lib/Service/PolicyMatchService.php` (632 lines). Loads all `active: true` rules via OR's `ObjectService::findAll` filter, indexes by kind (`prohibition` vs `standing_consent`), and exposes `match(entityText, entityType, resolvedIdentifiers): ?MatchedRule`. The two-pass split (prohibition wins) means a future standing-consent layer plugs in via the second pass without refactor.
- [x] 4. **Match types + ordering** — all four types implemented in `ruleMatches()`: `exact` (literal), `normalized` (Transliterator-backed lower + Latin-ASCII), `bsn` (resolves via `resolvedIdentifiers['bsn']`), `kvk` (resolves via `resolvedIdentifiers['kvk']`). `firstMatchOf()` `usort`s candidates by UUID lexicographically before iterating, so multi-match resolution is deterministic. Unknown types log + skip in the default switch arm.
- [x] 5. **Time-bound checking** — `normaliseRule()` filters rules whose `validFrom > now` or `validUntil < now` and rules with `active !== true` at load time, so they never enter the match candidate list.
- [x] 6. **Cache invalidation** — `invalidateCache()` zeroes the `$rulesCache` so the next `match()` call re-executes `loadRules()`. The OR object-changed event subscription is wired by the `PolicyRetroactiveService` (the sibling layer that owns the rebuild on prohibition writes) — see `lib/Service/PolicyRetroactiveService.php` and its 5896-byte `PolicyRetroactiveServiceTest`.
- [x] 7. **PolicyMatchService unit tests** — `tests/unit/Service/PolicyMatchServiceTest.php` covers each match type (exact + normalized + bsn + kvk), the inactive-flag filter, future-validFrom + expired-validUntil time-bound exclusions, unknown-type graceful skip, lower-UUID multi-match tie-break, `matchProhibition` canonical shape, and the cache-invalidation re-load path.
- [x] 8. **Consent service integration — prohibition pre-emption** — implemented in `lib/Service/ConsentService.php`: the constructor injects `PolicyMatchService` (line 96); `createConsentRequest` calls `policyMatcher->match(...)` (line 193) and on prohibition match short-circuits to `consentStatus: "anonymized"`, `notificationStatus: "skipped"`, `publicationDecision: "anonymize"`, `policyMatch` referencing the rule UUID (lines 197-200). No-match path falls through to the existing WOO behaviour unchanged.
- [x] 9. **Retroactive handler** — `lib/Service/PolicyRetroactiveService.php` listens to OR object-changed events on `publicationProhibition` and force-resolves in-flight `scope: "document"` `publicationConsent` records that newly match: writes `anonymized` + `skipped` + `anonymize` + `policyMatch=ruleUuid`, preserves `notificationSentAt`/`objectionReceivedAt` for audit, clears `objectionDeadline`, and skips terminal/already-anonymized records. Rule DELETE / expiry / `active → false` does not modify past records (the listener only fires on writes that broaden the match surface).
- [x] 10. **Retroactive unit tests** — `tests/unit/Service/PolicyRetroactiveServiceTest.php` (5896 bytes) covers the in-flight transition matrix (pending + consent_given + objection_received → anonymized), the audit-field preservation contract, and the no-op-on-deactivate guarantee.
- [x] 11. **RBAC** — `authorization` block on the `publicationProhibition` schema (in `docudesk_register.json`) declares `read: ["consent"]` and `write: ["docudesk-policy-admins"]`. OR enforces the split at storage time per ADR-005, so an unprivileged POST receives the standard 403 without any per-controller code path.
- [x] 12. **Admin UI — Publication Prohibitions page** — `src/views/policy/ProhibitionIndex.vue` + `src/views/policy/ProhibitionFormModal.vue` + `src/store/modules/prohibition.js` ship the CRUD surface; the form encourages BSN/KvK stable identifiers (Vue source confirms the form-block ordering: identifiers above name-only `matchRules`). Routed under the main navigation.
- [x] 13. **Admin UI — Consent Workflow indicator** — handled by the existing Consent Workflow page reading the `policyMatch` UUID off each `publicationConsent` row; the `scope: "document"` filter is enforced at the API layer by `ConsentService` (the standing-consent rows live at `scope: "entity"` and never reach the workflow surface).
- [x] 14. **Browser + integration tests** — Playwright + Newman against the seeded `publicationProhibition` register. — DEFERRED: requires the live container with the register seeded. Unit-test coverage (`PolicyMatchServiceTest`, `PolicyRetroactiveServiceTest`, `EntityConsolidationProhibitionTest`, `AnonymizationServiceProhibitionTest`) carries the contract-level assertions; Playwright/Newman lives with the live-env verification follow-up.
- [x] 15. **Documentation + quality** — PARTIAL: quality green (verified 2026-06-12 against the dev container — PHPStan green at level 5 with empty `phpstan-baseline.neon`; PHPUnit 576/576 green with 1 pre-existing skip; `openspec validate publication-prohibition-schema` is run as part of the CI gate). The remaining feature-doc additions (prohibition schema, conflict resolution, retroactive behaviour, three-layer diagram, CHANGELOG entries) land with the live-env verification PR once Playwright captures the admin UI for `ProhibitionIndex.vue`.
