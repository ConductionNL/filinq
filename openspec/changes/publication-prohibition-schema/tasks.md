## Tasks

- [ ] 1. **`publicationProhibition` schema** — add to `lib/Settings/docudesk_register.json` under the `consent` register's `schemas` block; properties: `primaryName`, `entityType` (enum PERSON/ORGANIZATION/OTHER), `matchRules` (array of `{type, value}` with type enum exact/normalized/bsn/kvk), `reason`, `legalAuthority`, `caseReference`, `severity`, `jurisdiction`, `addedBy` (user ref), `validFrom`, `validUntil`, `active`, `notes` (markdown); required: `primaryName`, `entityType`, `matchRules`, `reason`, `active`; verify the schema imports cleanly via `composer test:unit`.
- [ ] 2. **Seed data** — add four realistic `publicationProhibition` seed objects per design Seed Data: court order, minor protection, undercover officer, categorical privacy-board exemption; use the `@self` envelope per ADR-013 with stable slugs; verify seed import is idempotent (re-running the importer skips existing objects matched by slug).
- [ ] 3. **PolicyMatchService skeleton** — create `lib/Service/PolicyMatchService.php` (or extend the existing consent service); load at init: all `active: true` `publicationProhibition` records; index keys: `(matchType, entityType, value)` tuples for O(1) lookup; expose `match(entityText, entityType, resolvedIdentifiers): ?MatchedRule`; structure the cache so a future standing-consent portion (sibling change) plugs in without refactor.
- [ ] 4. **Match types + ordering** — implement `exact` (literal), `normalized` (case + accent strip via `Transliterator`), `bsn`, `kvk`; on multi-prohibition match, return the rule with the lowest UUID lexicographically (deterministic); reject unknown types (defence-in-depth — schema rejects at write; matcher logs + skips on unknown).
- [ ] 5. **Time-bound checking** — at match time, a rule with future `validFrom`, past `validUntil`, or `active: false` MUST NOT match.
- [ ] 6. **Cache invalidation** — subscribe to OR object-changed events; on `publicationProhibition` events, rebuild the prohibition portion of the cache; verify by inspection that a write triggers exactly one rebuild.
- [ ] 7. **PolicyMatchService unit tests** — each match type; time bounds; active flag; multi-rule precedence; multi-prohibition tie-break; cache invalidation on object-changed events.
- [ ] 8. **Consent service integration — prohibition pre-emption** — extend `ConsentService::createConsentRequest()` (or its caller) to consult `PolicyMatchService` before defaulting to the WOO workflow; on prohibition match: create a `scope: "document"` `publicationConsent` with `consentStatus: "anonymized"`, `notificationStatus: "skipped"`, `publicationDecision: "anonymize"`, `objectionDeadline: null`, `policyMatch` referencing the prohibition UUID; on no match: existing WOO behaviour unchanged.
- [ ] 9. **Retroactive handler** — on `publicationProhibition` INSERT or update making the rule match more entities (new matchRule, flip `active` to true, extend `validUntil`), find all in-flight `scope: "document"` `publicationConsent` records (status not in {`anonymized`, terminal published}) for matching entities and force-resolve to `anonymized` + `notificationStatus: "skipped"` + `publicationDecision: "anonymize"` + `policyMatch` referencing the new rule; cancel any pending notification dispatch (no-op if already sent); preserve `notificationSentAt` + `objectionReceivedAt` for audit even though `objectionDeadline` clears; on rule DELETE / expiry / `active → false`, do NOT modify past records.
- [ ] 10. **Retroactive unit tests** — in-flight `pending` and `consent_given` records transition correctly; `objection_received` records ALSO transition (prohibition overrides explicit objection); audit fields preserved; delete/expiry/deactivate does NOT modify past records.
- [ ] 11. **RBAC** — configure `publicationProhibition.authorization`: `read` open to consent group; `write` restricted to `docudesk-policy-admins`; document the install default in the schema's `authorization` block (admin can adjust via OR authorization UI); smoke-test that an unprivileged user cannot POST (403 expected per existing OR RBAC).
- [ ] 12. **Admin UI — Publication Prohibitions page** — new CnIndexPage view CRUD for `publicationProhibition`; use `@conduction/nextcloud-vue` components per ADR-012 (CnFormDialog, CnDataTable); create-form encourages adding stable identifiers (BSN/KvK) and warns loudly when only a name-based rule is added; routed and linked from main navigation.
- [ ] 13. **Admin UI — Consent Workflow indicator** — on the existing per-document Consent Workflow page, add a "policy pre-empted" indicator on rows whose `policyMatch` is non-null; apply a hard filter `scope: "document"` (so standing consents — sibling change — never appear here).
- [ ] 14. **Browser + integration tests** — Playwright: locked-on toggle for prohibition matches; Publication Prohibitions page CRUD; Consent Workflow indicator appears only on policy-matched rows; Newman: CRUD endpoints for `publicationProhibition`; four detection-time outcomes (no match → WOO; prohibition match → anonymized + policyMatch → prohibition); retroactive force-resolve (create in-flight `scope: "document"` with `pending`; create matching prohibition; verify transition).
- [ ] 15. **Documentation + quality** — add to `docs/features/publication-consent-process.md`: prohibition schema, conflict resolution, retroactive behaviour, UI toggle semantics, Publication Prohibitions admin page; diagram the three-layer model (prohibition → standing consent → workflow) and the four `policyMatch` × `notificationStatus` combinations; CHANGELOG "Added": `publicationProhibition` schema + Publication Prohibitions admin page; "Behavior changes": detection-time prohibition lookup may pre-empt the WOO workflow inside publication-clearance flows (generic anonymisation flows unaffected); `composer check:strict` clean (fix pre-existing issues in touched files); `openspec validate publication-prohibition-schema` clean.
