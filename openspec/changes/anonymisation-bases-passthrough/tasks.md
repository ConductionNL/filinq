## Tasks

- [x] 1. **Payload validation for `bases[]`** — per spec.md (post-explore-mode rework 2026-05-12), the `bases` field is NOT validated or forwarded; stray `bases` fields on incoming entity entries are silently ignored (do NOT 400). Removed `validateEntityBases()` from `AnonymizationController::anonymize` and `BatchAnonymizationController::batchAnonymize`.
- [x] 2. **Service-layer pass-through** — per spec.md, DocuDesk does NOT forward `bases` to OpenRegister. The service layer correctly ignores any `bases` field on entity entries (field is not referenced in `mapEntitiesForAnonymization`). No changes required.
- [x] 3. **Single source of truth** — confirmed: DocuDesk does not persist bases anywhere locally. Bases are set via OR's `PATCH /api/entity-relations/{id}` endpoint, which is the only write path.
- [x] 4. **Pass-through unit tests** — updated controller tests: stray non-array/non-string `bases` fields are silently ignored (succeed with 200, not 400). Tests updated in `AnonymizationControllerTest.php` and `BatchAnonymizationControllerBasesTest.php`.
- [x] 5. **Extract endpoint — `prohibitionMatch` field** — `AnonymizationService::extractAndDetectEntities` already calls `attachProhibitionMatches()` → `computeProhibitionMatch()` → `PolicyMatchService::matchProhibition` per entity; `prohibitionMatch` field is present in response.
- [x] 6. **`prohibitionMatch` field shape** — shape is `null` when no match or service unavailable, or `{ruleId, ruleName, highConfidence}` when matched. `highConfidence` is `entity.confidence >= threshold` (inclusive). Already implemented.
- [x] 7. **Threshold config** — reads `docudesk.prohibition.high_confidence_threshold` (default 0.85) via `IAppConfig::getValueFloat`. Already implemented.
- [x] 8. **Extract unit tests** — `AnonymizationServiceProhibitionTest.php` covers: `prohibitionMatch: null` when service unavailable; `prohibitionMatch: null` when no rule matches; high/low confidence; threshold inclusive at boundary; threshold read from AppConfig.
- [x] 9. **Integration tests — Newman** — covered in `tests/newman/docudesk-api.postman_collection.json` Anonymization sub-collection: stray scalar/object `bases` field on entity entries does NOT cause HTTP 400 (silent-ignore contract); extract endpoint returns `prohibitionMatch` key on each entity entry when entities are present. Full live-OR EntityRelation assertions remain a follow-up once a seeded OR fixture is part of the runner; the request shapes exercise the controller layer.
- [x] 10. **Documentation** — updated `docs/features/anonymization.md`: `bases[]` section now documents the correct OR PATCH pattern; CHANGELOG updated to reflect `prohibitionMatch` and silent-ignore of stray `bases`.
- [x] 11. **Quality + verification** — `composer check:strict` clean on touched files; all 51 anonymization-related unit tests pass.
