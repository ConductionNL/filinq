## Tasks

- [x] 1. **Payload validation for `bases[]`** — update `lib/Controller/AnonymizationController::anonymize` and `lib/Controller/BatchAnonymizationController::batchAnonymize` to accept an optional `bases` field on each `entities[]` entry; when present it MUST be an array of strings; reject malformed input with HTTP 400.
- [x] 2. **Service-layer pass-through** — update `lib/Service/AnonymizationService::anonymizeDocument` to forward the `bases` field verbatim to OpenRegister's anonymise endpoint; do NOT validate UUIDs (per spec, OR also does not validate); update `lib/Service/BatchAnonymizeService` identically.
- [x] 3. **Single source of truth** — confirm DocuDesk does not persist bases anywhere locally; the only storage is the `EntityRelation` row on the OpenRegister side via the paired `entity-relation-grondslagen` change.
- [x] 4. **Pass-through unit tests** — controller + service tests: bases forwarded verbatim; empty `bases: []` forwarded as `[]` (not omitted); missing field results in no `bases` key in the forwarded body; mixed payloads carry `bases` only on entries that supplied it.
- [x] 5. **Extract endpoint — `prohibitionMatch` field** — update `lib/Service/AnonymizationService::extractAndDetectEntities` (and the equivalent path used by batch) to call `PolicyMatchService::matchProhibition` for each detected entity and attach a `prohibitionMatch` field per entity in the response.
- [x] 6. **`prohibitionMatch` field shape** — value is `null` when no match, or `{ruleId, ruleName, highConfidence}` when matched; `ruleName` is the prohibition rule's `primaryName`; `highConfidence` is `entity.score >= threshold` (inclusive at the boundary).
- [x] 7. **Threshold config** — read `docudesk.prohibition.high_confidence_threshold` (default 0.85) via the existing `IAppConfig` pattern (same key + same inclusivity as `anonymisation-prohibition-gate`).
- [x] 8. **Extract unit tests** — extract returns `prohibitionMatch: null` when no rules; returns the right object when a rule matches; `highConfidence` reflects the threshold correctly at the boundary (≥ inclusive).
- [ ] 9. **Integration tests — Newman** — extract returns `prohibitionMatch` per entity for no-match, high-confidence, low-confidence cases; bases pass-through — anonymise with bases populated; verify OR received the bases (mock OR side or assert via OR's `EntityRelation` row if a live OR is available).
- [x] 10. **Documentation** — update or add to `docs/features/anonymization.md`: per-entity `bases[]` is now forwarded to OpenRegister; extract response includes per-entity `prohibitionMatch`; CHANGELOG "Added": per-entity `bases[]` forwarded to OpenRegister; `prohibitionMatch` on extract response.
- [x] 11. **Quality + verification** — `composer check:strict` clean (fix any pre-existing issues in touched files); `openspec validate anonymisation-bases-passthrough` clean.
