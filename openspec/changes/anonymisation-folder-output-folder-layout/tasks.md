## Tasks

> **All 10 tasks are DEFERRED to the same focused integration PR as `anonymisation-batch-output-folder-layout` tasks 3-13** (FolderBatchService + FolderExtractionJob consume the `OutputLayoutResolver` helper that landed in this batch with full unit-test coverage). The shared helper is in place; the call-site wiring + move-failure handling + path-recording + Newman + docs ship as one cohesive PR so the batch- and folder-flow layouts stay in sync.


- [~] 1. **FolderBatchService integration** — DEFERRED with `anonymisation-batch-output-folder-layout` task 3: the move-from-OR-output orchestration belongs in BatchAnonymizeService (where the OR `anonymizeDocument` call lives — `FolderBatchService` only runs entity *extraction*, not the anonymise/move step). The two specs ship as one cohesive PR so the move + path-recording + warning lifecycle stays consistent across batch- and folder-flow.
- [~] 2. **FolderExtractionJob integration** — DEFERRED with task 1: `FolderExtractionJob` only iterates over extraction (text + entity detection); no anonymise call happens here. The post-process belongs at the anonymise call-site (BatchAnonymizeService) and the source-discovery filter at the enumeration call-site (FolderBatchService, ✅ task 3).
- [x] 3. **Source-discovery filter** — `lib/Service/FolderBatchService.php::enumerateFiles()` injects `OutputLayoutResolver` and excludes any direct child whose base name ends with the legacy `_anonymized` suffix via the shared discriminator `OutputLayoutResolver::isLegacyAnonymizedOutput()` (one source of truth, no string drift). `FolderExtractionJob` does NOT need the same filter — it only iterates over files already queued by `FolderBatchService`, so the filter at enumeration time covers the folder-flow entry surface. Coverage: `tests/unit/Service/FolderBatchServiceTest.php::testEnumerateFilesExcludesLegacyAnonymizedOutputs` (mixed source set: clean files pass, `_anonymized`-suffixed ones are filtered) and `testEnumerateFilesAllLegacyOutputsThrows400` (re-run on a folder full of redacted copies surfaces the standard "no files" 400 rather than silently producing an empty batch).
- [~] 4. **Path-recording update** — DEFERRED with task 1: `anonymizedFilePath` shape change ships with the move that produces the new path; recording the move-failure legacy fallback needs the move-failure handler from the cohesive PR.
- [x] 5. **Single-file regression guard** — covered by `tests/unit/Service/AnonymizationServiceSingleFileRegressionTest.php` (shared with the sibling batch-layout change, see `anonymisation-batch-output-folder-layout` Task 7). The test asserts at the source level that `AnonymizationService` does NOT import or reference `OutputLayoutResolver` / `resolveBatchDestination` / `isLegacyAnonymizedOutput`, so the per-document anonymise endpoint stays untouched by the folder-flow rewrite. Fails CI on any future refactor that wires the helper into the per-document path.
- [~] 6. **FolderBatchService unit tests** — PARTIAL: source-discovery filter is covered (`testEnumerateFilesExcludesLegacyAnonymizedOutputs` + `testEnumerateFilesAllLegacyOutputsThrows400`, task 3 ✅). Post-process placement / subfolder reuse / collision / move-failure tests DEFERRED with task 1 — they exercise the move orchestration that has not yet landed.
- [~] 7. **FolderExtractionJob unit tests** — DEFERRED with task 2: the job is intentionally NOT the move call-site (see task 2 status); idempotency tests will land against whichever service ends up carrying the move (currently scoped to BatchAnonymizeService).
- [~] 8. **Integration tests — Newman** — DEFERRED with task 1.
- [~] 9. **Documentation** — DEFERRED with task 1: doc + CHANGELOG entries belong in the cohesive PR that produces the behaviour change.
- [~] 10. **Quality + verification** — local-env quality is GREEN (composer check:strict passes in the dev container; PHPStan green; PHPUnit 576/576 green incl. the two new source-discovery tests). Strict-CI run + live-stack smoke DEFERRED with task 1.
