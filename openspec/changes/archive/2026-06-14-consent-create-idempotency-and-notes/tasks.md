## Tasks

- [x] 1. **Idempotency on `(documentId, entityKey)`** — in `lib/Service/ConsentService::createConsentRequest()`, look up an existing `publicationConsent` by `(documentId, entityKey)` before creating; if found, route to the update path; if not found, create a new record.
- [x] 2. **Preserved-on-update fields** — on update, preserve workflow state (`notificationStatus`, `notificationSentAt`, `objectionReceivedAt`, `objectionReason`, `consentStatus` for non-pre-empted records); operator-set fields are updated (`legalBasis`, `notes` only the bracketed region, `contactEmail`, `contactAddress`); re-evaluate the pre-emption discriminator (`policyMatch` + `notificationStatus: "skipped"`) so the policy layer's current view is reflected.
- [x] 3. **Sentinel-tagged notes serialisation helper** — implement a helper that writes / replaces the bracketed region in `publicationConsent.notes` (begin/end HTML comments per design D3); preserves operator-authored content outside the brackets; on shrink-to-one-element, the bracketed region (plus any leading blank line it inserted) is removed entirely; regex matches single-line + multi-line via `s` flag.
- [x] 4. **First basis to `legalBasis`** — write `publicationBases[0]` to the existing `legalBasis` string field (max 500 chars); truncate at 500 with UI warning; remaining elements (basis 2..N) go through the sentinel helper into `notes`.
- [x] 5. **Notification dispatch stays stubbed** — reaffirm CONS-049: records created with `consentStatus: "pending"` carry `notificationStatus: "pending"` but the system MUST NOT auto-send any email or postal notification; the objection-deadline is still computed and recorded; real dispatch is a separate change.
- [x] 6. **ConsentService unit tests — idempotency** — new `(documentId, entityKey)` → create; existing → update operator-set fields; preserve workflow state across updates; pre-emption discriminator re-evaluated on update.
- [x] 7. **ConsentService unit tests — sentinel serialisation** — write with multi-element `publicationBases[]` produces the bracketed region; operator-authored content outside the brackets is preserved across re-submits; re-submit with the same multi-element array is a no-op on `notes` (idempotent rendering); shrink-to-one-element removes the bracketed region cleanly (including the leading blank line); shrink-to-zero (edge case if ever allowed) removes the bracketed region cleanly.
- [x] 8. **Integration tests — Newman** — direct `POST /api/consents` with the same `(documentId, entityKey)` twice: second call updates the existing record without creating a duplicate; multi-element `publicationBases[]` produces the bracketed region in `notes`; operator-authored notes content preserved; pre-emption preserved on re-submit when `policyMatch` was set on the original write.
- [x] 9. **Documentation** — extend `docs/features/publication-consent-process.md` with the idempotency contract (`(documentId, entityKey)`) and the sentinel-tagged notes serialisation (with the HTML-comment format and the "preserved operator content outside brackets" guarantee); CHANGELOG "Added": idempotent `createConsentRequest()` + sentinel-tagged additional-bases serialisation in `publicationConsent.notes`; "Behavior changes": re-submitting a duplicate `(documentId, entityKey)` now updates the existing record (was: 409/duplicate); existing operator-authored notes content is preserved across re-submits; the bracketed `<!-- docudesk:additional-publication-bases:* -->` region is auto-managed.
- [x] 10. **Quality + verification** — `composer check:strict` clean (fix pre-existing issues in touched files); manual smoke: direct `POST /api/consents` twice with the same key, verify update behaviour; verify sentinel-tagged region appears in `notes` for multi-basis input and shrinks cleanly; `openspec validate consent-create-idempotency-and-notes` clean.
