## Tasks

> BLOCKED_EXTERNAL (resolved 2026-06-14): OpenRegister's read-logging delta
> shipped (>= 0.2.14): `processing_log` table, `ProcessingLogService` reading
> the `x-openregister-processing` dialect (`logReads`/`attribution`/
> `subjectIdFields`), per-access read logging hooked into `ObjectService::find()`,
> and the FG/admin-gated `ProcessingLogController` query + per-subject
> (betrokkene) extract endpoints. DocuDesk's consumer build is complete against
> that surface. The OR-side pieces still deferred (full catalogue seeder
> OR-PA-2.2; aggregate Art. 30 JSON/CSV/PDF export OR-PA-5.3/7; OR
> verwerkingsregister UI OR-PA-7.1) keep the corresponding verification tasks
> marked `[~]` with a reason rather than faked.

- [x] 1. **Dependency check** — OR read-logging delta confirmed merged on `development` (`ProcessingLogService` / `ProcessingLogController`, `x-openregister-processing` dialect, OR `0.2.14`). Minimum OR version recorded as `^v0.2.14` in `lib/Settings/docudesk_register.json` (`x-openregister.openregister`) and as an XML comment in `appinfo/info.xml` (NC `info.xsd` has no versioned OR dependency element).
- [x] 2. **Catalogue annotations** — the four docudesk activities (`docudesk-anonymisation`, `docudesk-ocr`, `docudesk-metadata-enrichment`, `docudesk-signing`) declared as `x-openregister-processing` entries on `anonymizationLink`, `generatedDocument`, `base`, and `signingAuditEntry`: purpose (`doelbinding`), legal basis, NER data categories, backend identifier, `grondslagSource: EntityRelation.bases`, retention references from the existing `x-openregister-archival` annotations ("not declared" where absent), `lifecycle: draft`. Each opts the schema into read-logging (`logReads: true`) and attributes to its own code. Verified by `ProcessingActivityCatalogueTest` (5 tests, 510 assertions).
- [x] 3. **Admin compliance section** — Vue settings section in `src/views/settings/Settings.vue` (admin-only) that surfaces OR's controller-identity record with a configure prompt, lists the four activities, and deep-links to OR's per-access processing log + per-subject extract scoped to docudesk registers. English i18n source keys; `NcSettingsSection` + labelled controls (gate-12 clean). Build green.
- [~] 4. **Verification (catalogue)** — annotation contract (categories, retention references, read-log opt-in, self-attribution, OR version constraint) verified by `ProcessingActivityCatalogueTest`. The "register import seeds the four activities as drafts" leg is `[~]`: OR's full register-import catalogue seeder (OR-PA-2.2) is deferred — only a lazy per-org fallback activity is seeded today, so attribution falls through to OR's `niet-geclassificeerde-verwerking` bucket until the seeder lands. Rows without `bases` already surface in OR's unclassified bucket (OR-PA-4, shipped).
- [~] 5. **Verification (export + scope)** — the docudesk settings surface is covered by Playwright e2e (`tests/e2e/spec-coverage/admin-settings.spec.ts`, UI only). The OR-side aggregate Art. 30 export with the no-literal-PII byte contract is `[~]`: OR's `Art30ExportService` (OR-PA-5.3/7) is deferred — only the per-access read log + per-subject (betrokkene) extract ship today (both FG/admin-gated, fail-closed, range-bounded, no cross-tenant IDOR, asserted in OpenRegister's `ProcessingLogControllerTest`, referenced not duplicated). Non-admin denial is OR-PA-8 (shipped). The compliance section honestly notes the aggregate export is forthcoming.
- [x] 6. **Documentation** — `docs/features/processing-activity-export.md` added (capability provided by OpenRegister; docudesk contributes the catalogue; Art. 30 mapping table; honest capability-status section). `docs/GOVERNMENT-FEATURES.md` gains row F-12a marked *In ontwikkeling* (not *Beschikbaar* — aggregate export OR-deferred, no live verify against the dev container in this build).
- [x] 7. **Quality gates** — `php -l` clean on changed PHP; PHPUnit unit suite green (755/755 with intl; the 11 `Transliterator`/intl errors in the bare image are pre-existing environment limitations, not regressions, and not in touched files); register JSON valid; ESLint 0 errors on the changed Vue (pre-existing JSDoc warnings only); webpack build green; hydra gates green for the diff. CHANGELOG entry added.
