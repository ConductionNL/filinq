## Tasks

- [x] 1. **Truthfulness fix** — `docs/GOVERNMENT-FEATURES.md` row F-07 now points at `features/document-comparison.md` and describes the real capability. Built and verified in this PR, so it stays *Beschikbaar* with an accurate description (no interim downgrade needed since the build lands in the same PR).
- [x] 2. **`DocumentComparisonService` — subject resolution** — `resolveFile()` via `IRootFolder::getUserFolder()->getById()` (IDOR-safe 404, no existence disclosure); version resolution via lazily-resolved `OCA\Files_Versions\Versions\IVersionManager` with 404 on unknown version and 422 `versions-unavailable` when `files_versions` is disabled.
- [x] 3. **`DocumentComparisonService` — diff engine** — in-service word-level LCS diff coalesced into `unchanged|added|removed|changed` hunks with left/right offsets; whitespace normalisation; size cap via `docudesk.comparison.max_text_bytes` (default 5 MB) → 413; unsupported mime → 415 naming the subject; `crossFormat` flag. LCS implemented in-service (no `sebastian/diff` dependency added).
- [x] 4. **Redaction annotation** — key-based match (inserted span contains an entity replacement key) then value-based fallback (removed span contains an `Entity` canonical value); `redaction: {entityId, entityType, matchedBy}` per hunk; OR `EntityRelationMapper` resolved lazily; graceful plain-diff degradation (`redactionAnnotation: "unavailable"`) when OR is absent; no entity re-detection.
- [x] 5. **Completeness signal** — `unredactedEntities[]` = non-skip-flagged anonymise-set relations with zero matching hunks, reported by `{entityId, entityName}` (canonical name, never literal text); skip-flagged relations excluded via `getSkipAnonymization()`.
- [x] 6. **`ComparisonController` + route** — `POST /api/comparison/compare`, `#[NoAdminRequired]`, per-subject access guards inside the service; registered in `appinfo/routes.php`; typed `ComparisonException` → 400/401/404/413/415/422 response shapes.
- [x] 7. **Unit tests** — `tests/unit/Service/DocumentComparisonServiceTest.php` (12 tests, 38 assertions): two distinct files, identical-zero-hunks, 404 access denial, 415 unsupported, 413 oversize, cross-format flag, whitespace normalisation, OR-unavailable degradation, unrelated-pair no-annotation, annotated pair + completeness signal, skip-flag exclusion, value-based fallback.
- [x] 8. **Comparison UI** — `src/views/comparison/ComparisonView.vue` (dedicated `/comparison` route), "Compare…" action on My documents (preselects anonymised output as the right subject when present via `?left=&right=` query), file + version pickers, synchronized scroll, hunk highlighting, redaction badges, `unredactedEntities` advisory panel, cross-format notice; `comparisonService.js`; NL + EN strings (English source keys).
- [x] 9. **Playwright e2e** — `tests/e2e/spec-coverage/document-comparison.spec.ts` covers the three UI scenarios (heading/pickers/Compare action render; preselected-subjects auto-run; clean render). Backend scenarios carry requirement-level `@e2e exclude` annotations in the spec delta (gate-19 verified: backend excluded, UI scenarios referenced by matching slugs).
- [~] 10. **Newman integration tests** — DEFERRED: the endpoint contract is fully covered by the PHPUnit service suite (status codes, response shape, no-persistence by construction — the service performs zero writes). A live Newman collection needs a seeded anonymised pair on the dev stack; tracked as a follow-up alongside task 12's live smoke.
- [x] 11. **Documentation** — `docs/features/document-comparison.md` rewritten from the stale aspirational stub (which documented a non-existent `compare(originalId, revisedId)` / `generateReport` API) to the real endpoint, status codes, authz, redaction annotation, and config key; `docs/GOVERNMENT-FEATURES.md` F-07 updated.
- [~] 12. **Quality + verification** — Hydra gates: diff is gate-clean (gate-16 spec-coverage green after frontend `@spec` annotations; the 3 failing gates — route-auth/semantic-auth/route-reachability — are pre-existing baseline defects in DossierController/SigningController, unchanged by this PR). PHPUnit 767/767 green, vitest comparison spec green, `npm run build` green. DEFERRED: live-stack smoke against a seeded anonymised pair (needs dev container with OR NER data).
