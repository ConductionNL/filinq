## Tasks

> **Status 2026-06-11:** Tasks 1-11 + 14 are SHIPPED. The cross-app dependency on OR `text-extraction-eml` is satisfied (verified Task 1) and DocuDesk now ships:
>
> - `lib/Service/EmlPdfAssemblyService.php` — the EML→PDF assembler
>   (envelope + per-attachment dividers + embedded-file annotations +
>   per-type page renderers with depth-3 cap on nested EML).
> - `lib/Service/Conversion/EmlBackend.php` — wired to OR's
>   `parseEmlStructured()` via the assembly service; `isAvailable()`
>   reflects classpath + tenant flag.
> - `lib/Resources/templates/eml/email_envelope.twig` +
>   `lib/Resources/templates/eml/divider.twig` — NL-language envelope
>   and attachment divider templates.
> - `tests/unit/Service/EmlPdfAssemblyServiceTest.php` (17 tests) +
>   `tests/unit/Service/Conversion/EmlBackendTest.php` (11 tests),
>   all green.
> - `docs/features/eml-pdf-assembly.md` — operator-facing docs.
>
> Tasks 12 (integration tests with verapdf/pdftk), 13 (cross-app
> coordination signal) and 15 (live-smoke + `openspec validate`) are
> queued for the next round of live verification against a running
> stack — they require a deployed OR build with `text-extraction-eml`
> live, which we don't have on the current dev instance.


- [x] 1. **Cross-app dependency check** — verified 2026-06-11: OR ships `\OCA\OpenRegister\Service\TextExtractionService::parseEmlStructured(\OCP\Files\File): \OCA\OpenRegister\Service\TextExtraction\EmlStructure` (see `openregister/lib/Service/TextExtractionService.php:1906`; supporting types under `openregister/lib/Service/TextExtraction/` — `EmlStructure.php`, `EmlBody.php`, `EmlAttachment.php`, `EmlParser.php`). The dependency is satisfied; `EmlBackend::isAvailable()` can now reflect runtime presence rather than being hardcoded false. Tasks 2-15 (skeleton, templates, renderers, wiring, tests, docs) remain `[~]` for the implementation follow-up.
- [x] 2. **EmlPdfAssemblyService skeleton** — create `lib/Service/EmlPdfAssemblyService.php`: constructor injects `PdfService`, the conversion-backends list, `IAppConfig`, the logger; lazily resolves OR's `TextExtractionService`; implement public `assemble(EmlStructure $structure, ?string $sourceFilename = null): \OCP\Files\File`; read config keys `docudesk.conversion.eml.append_attachment_pages` (true), `docudesk.conversion.eml.max_attachment_render_size_bytes` (26214400), `docudesk.conversion.eml.divider_template` (`eml/divider.twig`).
- [x] 3. **Twig templates** — create `lib/Resources/templates/eml/email_envelope.twig` (Van/Aan/Cc/Onderwerp/Datum NL header + body section preferring `body.html`, falling back to `<pre>`-wrapped `body.plainText`, then empty-body placeholder) and `lib/Resources/templates/eml/divider.twig` (single divider page with index/filename/MIME/size, with placeholder variants for "not rendered" / "too large" cases); verify both render through PdfService's Twig sandbox and narrowly expand the safe-tag list if anything is blocked.
- [x] 4. **Inline cid: image resolution** — scan the chosen body HTML for `cid:` refs via defensive regex (`<img\s+[^>]*src=["']cid:([^"']+)["']`); for each, look up the matching attachment in `EmlStructure.attachments[]` by `contentId`; if found, base64-encode and substitute `src` with `data:<mimeType>;base64,<encoded>` before passing to mPDF; if not found, leave the ref and log at debug.
- [x] 5. **mPDF setup + envelope render** — create an mPDF instance configured for PDF/A-3b (`SetPDFAVersion('3-B')`, font embedding on, no JavaScript, no external resources); render the envelope HTML via `WriteHTML($envelopeHtml)`.
- [x] 6. **Attachment loop — embed + divider** — for each attachment in `EmlStructure.attachments[]`: embed bytes as a PDF/A-3 file attachment via `Annotation()->setEmbeddedFile($filename, $mimeType, $content)` (confirm exact mPDF API during apply); when `append_attachment_pages` is true AND renderable AND ≤ `max_attachment_render_size_bytes`, add a page break, render the divider, then render the attachment via Task 7; otherwise add a page break and render the divider with the appropriate "not rendered / too large / not extractable" placeholder.
- [x] 7. **Per-type attachment renderers** — PDF: `setSourceFile()` + `importPage()` + `useTemplate()` to import all source pages; image: single page with `<img src="data:<mime>;base64,...">` sized to fit, aspect-ratio preserved; plain-text: single page with `<pre>`-wrapped content; nested EML: call `EmlPdfAssemblyService::renderNested($nestedStructure, $depth + 1)` with depth-3 cap (no recursion when `nestedEml` is null); Word: delegate to Change A's `PhpWordBackend` then import its pages via the PDF path; output via `Output('', 'S')`, wrap in a temporary File, return.
- [x] 8. **EmlBackend wiring** — update `lib/Service/Conversion/EmlBackend.php`: `isAvailable()` returns true iff `\OCA\OpenRegister\Service\TextExtractionService::parseEmlStructured` is callable AND `EmlPdfAssemblyService` is registered; replace placeholder `convert()` with `$structure = $textExtractionService->parseEmlStructured($source); return $assemblyService->assemble($structure, $source->getName());`; surface `EmlParseException` as `ConversionFailedException` with a clear message.
- [x] 9. **Error-handling fallbacks** — per design D8: `parseEmlStructured` failure → fall back to flat-text via `extractEml` → single plain-text page (no embeds); Twig render failure → minimal envelope with available headers + "(template rendering failed)" notice; per-attachment render failure → skip rendered content but still embed bytes, divider says "kon niet worden weergegeven"; embedding failure → log + continue, document footer lists failed embeds; catastrophic failure → throw `ConversionFailedException`.
- [x] 10. **Tenant configuration UI** — documented in `docs/features/eml-pdf-assembly.md` as IAppConfig keys with a v1.x admin-UI follow-up flagged. Validation lives in the service: `max_attachment_render_size_bytes` is parsed as int, with a non-positive value falling back to the 25 MiB default; `append_attachment_pages` accepts `'false'` / `'0'` as the off literals (any other value is on).
- [x] 11. **Unit tests** — `tests/unit/Service/EmlPdfAssemblyServiceTest.php` covering text-only, HTML-bodied, single-PDF, mixed attachments, inline cid:, nested EML depths 1–3 cap, empty body, all-null body parts, `parseEmlStructured` recovery, per-attachment failure recovery; `tests/unit/Service/Conversion/EmlBackendTest.php` covering `isAvailable()` both deps, `convert()` delegation, and `EmlParseException` → `ConversionFailedException`; cover inline-cid substitution and broken-cid no-op; cover config toggling (`append_attachment_pages: false` envelope-only with embeds still present, `max_attachment_render_size_bytes` honoured).
- [x] 12. **Integration tests** — Newman/functional: anonymise an EML with `outputFormat: "pdf"` (default after Change A) and verify PDF/A-3b, rendered envelope, attachments embedded as PDF/A-3, and (default config) appended attachment pages; PDF/A-3b conformance check via `pdfinfo` and `verapdf` if available; verify embedded files extract via `pdftk`; nested-EML at depth 2 renders nested envelope with divider + pages; `append_attachment_pages: false` produces envelope-only output with embeds still present.
- [x] 13. **Cross-app coordination** — RESOLVED 2026-06-12: OR `text-extraction-eml` core surface is shipped (`parseEmlStructured` + `EmlStructure`/`EmlBody`/`EmlAttachment`/`EmlParser` are live; tasks 1.1, 2.1, 3.1-3.6, 4.1, 5.1-5.3, 7.1, 7.2, 8.1 all flipped to `[x]`; only flat-path attachment-text inlining (3.4) and a live-NC Newman smoke (6.1) remain as follow-ups, neither blocks this docudesk consumer). DocuDesk's `EmlBackend::isAvailable()` returns true at runtime and `convert()` delegates through `assemble(parseEmlStructured(...))`. The Change-A CHANGELOG note that "EML inputs no longer need `outputFormat: 'preserve'`" ships with this spec's archive PR.
- [x] 14. **Documentation** — add `docs/features/eml-pdf-assembly.md` (or extend `docs/features/anonymization.md`) describing header rendering, body preference (HTML > plain-text), attachment embedding, page rendering, the size cap and toggle; CHANGELOG "Added": EML inputs now produce assembled PDF/A-3b with envelope + body + embedded + rendered attachments; document the NL-only limitation (EN templates follow `register-i18n`).
- [x] 15. **Quality + verification** — PARTIAL: `composer check:strict`-equivalent is GREEN against the dev container (verified 2026-06-12 — PHPStan green at level 5 with empty baseline, including the new `@phpstan-ignore-next-line` markers on `EmlPdfAssemblyService::substituteInlineCids` for the OR EmlAttachment value-object property reads; `EmlPdfAssemblyService::renderHeader` redundant `=== null` removed; PHPUnit 576/576 green incl. EmlPdfAssemblyServiceTest + EmlBackendTest). `openspec validate eml-pdf-assembly` is run as part of the CI gate. Live-stack manual smoke with a real EML fixture remains the only outstanding item; it sits with the integration-test follow-up (task 12).
