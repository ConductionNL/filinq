schema: spec-driven

context: |
  Project: DocuDesk
  Repo: ConductionNL/docudesk
  Type: Nextcloud App (PHP backend + Vue 2 frontend)

rules:
  proposal:
    - Consider GDPR compliance implications
  specs:
    - Include GDPR/AVG article references
  design:
    - Uses OpenRegister services for all data operations
  tasks:
    - Test with OpenRegister
